# Claude Memory Candidates

Draft memory-card candidates here. Do not write directly to `.ai/memory/` during active competition work.

| Claim | Scope | Evidence | Counter-evidence | Predicted impact | Status |
|-------|-------|----------|------------------|------------------|--------|
| **Independently-validated shift-robustness levers on orthogonal axes (model-family swap, train-side augmentation) compose roughly ADDITIVELY when applied together, in contrast to this same competition's LGBM+MLP blend family where combining components was harmful/anti-additive relative to the better single model.** exp_042 (own) applied exp_037/038's `more_oversample` rare-class augmentation (validated on LGBM, topq +0.0206 vs LGBM-no-augment) to exp_040's XGBoost-alone (validated model-family swap, topq +0.0257 vs LGBM-no-augment) and measured the shift-aware topq delta on top of the already-improved XGBoost baseline: +0.0086 (full 3-fold CV, all 3 metrics positive: plain +0.0056, weighted +0.0315, topq +0.0086). Total vs the original LGBM-no-augment reference: 0.0257+0.0086=0.0343 measured, matching the sum of the two independent deltas almost exactly (no interaction penalty, no bonus). | any competition where two or more shift-robustness/accuracy levers have each been independently validated via full CV on the SAME shift-aware metric -- worth testing whether stacking them composes additively before assuming diminishing returns or (per this competition's blend-family experience) actively harmful interaction | exp_040 (model family, +0.0257) + exp_042 (augmentation on top of XGBoost, +0.0086) = +0.0343 measured total vs baseline; contrasts directly with sub_022/024 (LGBM+MLP blend, CV improved but LB dropped -0.02 to -0.04 vs LGBM-alone) | none yet within this competition; only one instance of composing two validated levers has been tried here -- would want a second pair (e.g. CatBoost + augmentation, if exp_043 confirms CatBoost as a 3rd viable model family) to confirm this isn't a one-off | MEDIUM-HIGH -- when two shift-robustness levers are each independently confirmed on the SAME shift-aware metric and act on different axes (model family vs. training data), stacking them is worth a cheap probe before assuming the gain caps out; the key distinguishing factor from the blend-family failure may be that model-family-swap + augmentation don't change WHICH rows drive the prediction the way a soft-vote blend of two different classifiers' probability outputs does | drafted this block (exp_042), full-train fit in progress, pending LB confirmation |
| **A shift-aware holdout metric (most-test-like-quartile accuracy via adversarial P(is_test)) can rank submission candidates in the OPPOSITE direction from raw local CV, and when they disagree, the shift-aware metric is the one that predicts the public LB direction.** sub_025 (exp_042, XGBoost+more_oversample-aug) has lower raw local CV (0.9463) than sub_024 (MLP-blend, 0.9523) but scored public 0.94252 vs sub_024's 0.89655 -- LB ranks them in the opposite order from raw CV, and the shift-aware topq metric (which correctly favored exp_042: topq +0.0343 vs the sub_020 baseline, vs the blend family's negative/near-zero topq gains) called it right. The automated score gate (`submit.py`, compares raw `local_score` only) would have rejected this submission by default; required a manual `--force` override to submit, logged transparently as a C1 ledger event with the justification recorded in the submission description. | any competition where an adversarial-validation-derived reweighted holdout metric has been established as more trustworthy than raw CV (i.e. after measuring low CV-LB sign agreement per `verifiers.py cv-lb`, as this competition's coordinator did) -- once that's established, submission gating should switch to comparing against the shift-aware metric's best, not raw CV's best, and the automated score-gate tooling should be updated to support this rather than requiring per-submission manual override | sub_020/022/024/025 four-point comparison this competition: every blend-family submission (022/024) underperformed a same-family non-blend baseline (020) on LB despite higher CV; the one submission gated on the shift-aware metric instead of raw CV (025) beat all of them on LB despite NOT having the highest raw CV | none yet -- only one shift-aware-vs-raw-CV disagreement has been submission-tested so far this competition; would want a second instance (ideally from gemini/codex, on a different feature set) to rule out this being specific to the MLP-blend family's particular failure mode | HIGH -- directly actionable and now has out-of-sample confirmation (the metric was established from sub_020/022/024's pattern BEFORE sub_025 was submitted, then correctly predicted sub_025's direction relative to sub_024) | drafted this block, one confirming data point (sub_025), recommend as a candidate for shared promotion once cross-reviewed |
| For pitch/fundamental-frequency classification tasks, dense candidate-frequency-grid harmonic-evidence features (score harmonic energy + ACF/cepstrum agreement per candidate f0) beat generic spectral/timbre summary stats (MFCC/chroma/contrast/ZCR) by a wide margin -- not a marginal tuning gain but a ~5x probe-scale OOF jump (0.588 vs 0.117-0.151) and the difference persists to LB (sub_020 grid-only 0.91954 LB vs earlier generic-feature approaches in the 0.6-0.7 CV range for both agents). Converged independently: claude's own exp_005 implementation and gemini's `extract_all_features_norm.py` (cross-reviewed sub_018) reached the same design from different starting points. | audio pitch/f0 classification tasks (missing-fundamental / harmonic inference specifically; likely generalizes to any task where the label is "which frequency is present" rather than "what does this sound like") | exp_005 full CV 0.9086 (fold std 0.0147) vs exp_002 generic-feature LGBM 0.5682; sub_020 kaggle_score 0.91954, LB *above* local CV (unusual, see next row); gemini's parallel implementation of the same feature family also became their best-performing feature set across sub_017-021 | none yet -- only one competition observed; would want a second f0/pitch-classification competition to confirm this isn't specific to this dataset's recording setup | HIGH -- reorder feature-engineering priority for audio pitch tasks to try candidate-grid harmonic-evidence features before generic spectral/chroma stats | drafted this block, not yet promoted |
| Ensemble blend-weight optimizers (COBYLA/scipy.minimize on plain OOF accuracy) will re-weight toward shift-prone features even when a shift-robust feature family is available in the same blend, because the optimizer only sees in-sample OOF error and the measured CV-LB sign agreement is only ~54% under the shift -- so "more models in the blend" does not fix a distribution-shift problem and can dilute a shift-robust submission's LB score by re-introducing the shift-prone signal at a large weight | any competition with a confirmed train/test distribution shift (adversarial AUC >> 0.6) where some feature families are more shift-robust than others | cross-review of gemini sub_021 (`.ai/reviews/claude-reviews-gemini-sub_021.md`): blend gives 61% weight to shift-prone baseline-family models vs 39% to shift-robust grid-only models, CV 0.9695 -> LB 0.8908 (gap 0.079); meanwhile claude's sub_020 (100% shift-robust grid features, 0% shift-prone blend weight) scores LB 0.91954, higher than gemini's 9-model blend despite lower CV. **Self-tested this block (exp_011)**: direct blend-weight grid search (grid proba vs RMS-normalized-generic proba) on claude's own OOF, scored both by plain accuracy and by exp_006's shift-aware (adversarial-p-test-weighted) accuracy -- BOTH metrics are monotonically increasing in grid-weight and agree exactly that w=1.0 (100% grid, 0% generic) is optimal. So the failure mode isn't "the optimizer picks worse than a shift-aware one would" (they agree here) -- it's that blending in a shift-prone family has no accuracy floor to offer once a strong shift-robust family is available; it can only dilute. | none yet on the "optimizer picks differently under shift-aware scoring" sub-claim (falsified in the one direct test run: both scorers agreed) -- but the broader "don't blend shift-robust with shift-prone" claim has zero counter-evidence across 2 independent tests (gemini's observational blend, claude's own controlled ablation) | HIGH for the broader claim (don't blend a shift-prone family into a shift-robust one, at any weight) -- MEDIUM-LOW specifically for "use shift-aware scoring to pick the weight," since claude's controlled test found plain OOF scoring reached the same conclusion here | drafted prior block, self-tested this block (exp_011), ready to promote the broader claim |
| A tiny-row-count probe subsample (e.g. 300 rows / a few per class) can predict the WRONG SIGN for LGBM capacity hyperparameter changes (num_leaves, min_child_samples) on high-class-count multiclass problems -- the direction that looks good on a small subsample can reverse at full data scale, in both directions (shallower AND deeper than baseline). Raising probe fidelity by cutting fold-count instead of row-count (single stratified holdout on full data) fixed the mismatch and agreed with the expensive full 3-fold CV at a fraction of the cost. | any multiclass problem with many classes (~80+) and modest rows/class (~20-30), when probing LGBM capacity hyperparameters on a small row-count subsample | exp_010: 300-row probe predicted shallower leaves (7 vs 31) would help by +0.079; full 2330-row 3-fold CV showed it hurt by -0.0043. exp_012: full-data single-holdout probe (no row subsampling, just 1 split instead of 3) correctly showed BOTH shallower and deeper (63, 127) hurt vs baseline (31) -- matching the direction the expensive full 3-fold CV would show, at ~485s/config instead of needing a full multi-fold run per config | none yet -- only tested on this competition's grid-harmonic feature set; would want to see if it holds for low-class-count or larger-rows-per-class problems where subsampling probably doesn't distort the capacity/regularization tradeoff as much | MEDIUM-HIGH -- for high-class-count multiclass, prefer "full rows, fewer folds" over "few rows, full folds" when probing LGBM capacity hyperparameters; row-count subsampling can silently starve rare classes below the point where regularization direction is measurable | drafted this block, self-tested (exp_010 -> exp_012 was the direct fix), ready to promote |
| Once a hand-engineered feature family is already strong (e.g. exp_005's grid-harmonic features, probe OOF 0.588), bolting on MORE engineered variants of the same family (extra features computed differently, not new information) tends to hurt a tree model rather than help, even when each variant is individually well-motivated and even when the variant is targeted at a specific, confirmed error mode rather than a generic elaboration. Four separate attempts across the competition (exp_007: + generic spectral/chroma stats, probe delta -0.062; exp_014: widened ACF/cepstrum lag window, probe delta -0.021; exp_015: + temporal-windowed harmonic stats, probe delta -0.017; exp_035: a per-candidate octave-neighbor ratio feature specifically targeting exp_034's confirmed octave-confusion error mode, probe delta +0.0172 but FULL 3-fold CV delta -0.0051 on the shift-aware topq metric -- reversed sign) all failed. exp_035 is the full-scale confirmation the earlier 3 (probe-only) lacked: this is not just a probe-fidelity artifact, a genuinely well-motivated, mechanism-specific feature addition still lost at full scale. Likely mechanism: LGBM's split budget gets diluted by additional correlated/noisier columns rather than gaining new separable signal, and this holds even when the new column is theoretically well-targeted. | LGBM/tree-based multiclass models with an already-strong, information-dense hand-engineered feature family; probing whether to ADD more feature variants vs keep the existing set | exp_007 (probe -0.0619), exp_014 (probe -0.0206), exp_015 (probe -0.0172), exp_035 (probe +0.0172, full topq -0.0051) -- 4/4 negative at the decision-relevant fidelity, including the one variant that was carried all the way to a full, shift-aware-scored CV | none -- 4/4 across two different motivating rationales (generic elaboration vs targeted error-mode fix) and two different fidelities (probe-only and full-CV-confirmed) | HIGH -- once a feature family scores well, prefer trying it as-is at full scale over iterating variants, REGARDLESS of how well-motivated the next variant seems (a confirmed error-mode target, like exp_035's octave confusion, is not sufficient reason to expect success); treat this family of ideas (more grid-harmonic variants) as closed for this dataset | promoted -- 4/4 negative including one full-scale, shift-aware-metric confirmation |
| The adversarial-weighted-quartile shift-robustness check (exp_006's methodology) can give a clean bill of health to a model/blend that STILL suffers a large real CV-LB drop -- i.e. a clean quartile gap is necessary-looking evidence but not sufficient. sub_022 (LGBM+MLP blend, OOF 0.9476, proper 3-fold CV, fold std 0.0088) scored LB 0.87931, worse than sub_020's LB 0.91954 (LGBM-only, OOF 0.9086) -- a real -0.04 CV-to-LB drop. exp_020 (own, this block) directly tested the natural hypothesis (MLP is more shift-fragile than LGBM on the identical grid features) by applying exp_006's exact quartile-gap diagnostic separately to oof_lgb_exp017.npy, oof_mlp_exp017.npy, and the blend: gap_lgb=-0.0087, gap_mlp=-0.0129, gap_blend=-0.0078 (the BLEND has the smallest/least-negative gap of the three, all in the same -0.008 to -0.013 range as exp_006's original LGBM-only reference of -0.0095). The hypothesis is falsified -- MLP is not meaningfully more shift-fragile by this measure, and the diagnostic doesn't even point toward the blend being risky. | any competition with a confirmed train/test distribution shift, when using an adversarial-quartile-gap check (or similar in-train-only shift diagnostic) as a gate before trusting a CV gain from a new model/blend | sub_020 LB 0.91954 vs sub_022 LB 0.87931 despite CV 0.9086->0.9476 (fold std 0.0088, clean, not a probe-fidelity artifact); exp_020's quartile-gap diagnostic applied per-family found no meaningful difference (gap_lgb=-0.0087, gap_mlp=-0.0129, gap_blend=-0.0078) and did not predict the LB drop in either direction | none of the three gaps measured is far from exp_006's reference -0.0095, so there is no internal counter-evidence -- the finding IS the counter-evidence to the original (row now superseded) hypothesis that this diagnostic would explain the drop | HIGH (methodology caution) -- an adversarial-quartile-gap check only uses TRAIN rows reweighted by test-likeness; it cannot see whatever property of the actual held-out TEST rows is causing the real gap, so treat a clean quartile-gap as weak evidence at best, not a green light, especially for ensembles/blends. The mechanism behind sub_022's specific drop remains unexplained -- do not assume it's understood just because this one diagnostic came back clean. | superseded the exp_020-pending version this block; exp_020 landed, hypothesis falsified, ready to promote as a methodology-limitation card |
| **exp_023 strengthens the row above**: applied the same adversarial-test-likeness reweighting not just as a per-model gap check (exp_020) but as the actual DECISION criterion for choosing the LGBM/MLP blend weight -- grid-searched w_lgb in [0,1] on exp_017's saved OOF, scored by plain accuracy, test-likeness-weighted accuracy, and most-test-like-quartile accuracy. All three metrics agree exactly: w=0.4 is optimal (topq_acc 0.9554 at w=0.4 vs 0.9177 at w=1.0). If this diagnostic had been used to CHOOSE the blend weight before submitting sub_022, it would have picked the same w=0.4 that lost -0.04 on LB -- not steered toward sub_020's safer w=1.0. This closes off "use shift-aware/quartile-reweighted scoring as a blend-weight selection criterion" as a fix for this specific failure mode (C9): it's not that the diagnostic wasn't applied as a decision rule yet, it's that applying it as one would not have helped. | any competition with a confirmed shift, when tempted to use an adversarial-test-likeness-reweighted CV metric (not just a diagnostic check, but as the actual weight/model-selection objective) to defend an ensemble against the sub_022-style CV-up/LB-down failure | exp_023: topq_acc(w=0.4)=0.9554 > topq_acc(w=1.0)=0.9177, shift_wtd_acc also peaks near w=0.3-0.4, not w=1.0 -- three separate in-train shift-reweighting metrics (plain, shift-weighted, top-quartile) all pick the LB-losing weight | none -- 3/3 in-train diagnostics tried this competition (exp_006 gap check, exp_020 per-model gap, exp_023 weight-search-as-objective) failed to predict or prevent the sub_022 drop | HIGH (methodology caution, escalated from row above) -- for this dataset's specific shift, no adversarial-reweighting-based in-train metric tested so far substitutes for an actual LB probe; treat any CV-only defense against this failure mode as unproven, and prefer holding back genuinely uncertain ensemble gains for a real (budgeted) LB check rather than trusting a clean shift-aware score | drafted this block (exp_023), ready to promote alongside the row above |
| A candidate (unconfirmed, no true test labels) mechanism for the sub_022 CV-up/LB-down drop: comparing sub_020 (LGBM-only) vs sub_022 (LGBM+MLP blend) test-set predictions directly (exp_022, purely descriptive, no retraining) shows the blend systematically shifts 13.7% of test predictions away from rare (<10-train-sample) classes and toward mid-frequency classes relative to LGBM-only (of the disagreement rows, LGBM-only picks a rare class 21.25% of the time vs the blend's 8.75%; the blend's overall predicted-class distribution is also closer to the train class prior, L1=0.365 vs LGBM-only's 0.410). MLP's smoother decision boundary appears to correct LGBM's rare-class overcalls in a way that helps in-distribution (CV, which shares train's class balance) but may hurt on the actual shifted test set if LGBM's rare-class calls were disproportionately correct despite being individually rare. | any competition with confirmed train/test shift + class imbalance, where a CV-validated blend/ensemble unexpectedly drops on LB relative to a simpler single-model submission | exp_022: 80/583 test-row disagreements between sub_020 and sub_022, rare-class share of disagreements 21.25% (LGBM side) vs 8.75% (blend side); L1-to-train-prior 0.410 (LGBM) vs 0.365 (blend) | none yet -- cannot be confirmed without true test labels (post-competition-close writeup or private LB reveal would be the only way); treat as a hypothesis to re-examine at postmortem, not an actionable rule yet | LOW-MEDIUM until confirmed -- do not act on this alone (e.g. do not deliberately bias a model toward more rare-class calls) without a labeled validation signal; useful mainly as a candidate explanation to check against at postmortem | drafted this block (exp_022), explicitly unconfirmed, not ready to promote |
| **A soft-vote blend's LB behavior tracks its dominant-weight component almost 1:1, even at a "moderate" mixing weight** -- exp_031 (own, descriptive, no true labels) compared test predictions from sub_020 (LGBM-alone, LB-best 0.91954), sub_024 (LGBM+MLP blend at w_lgb=0.4, LB 0.89655), and exp_030 (MLP-alone, not yet LB-tested, prepped this block). At w_lgb=0.4 the blend is actually 60%-MLP-weighted, and its predictions show it: blend-vs-MLP-alone disagreement is only 9.4% vs LGBM-alone-vs-MLP-alone's 21.8%; on the 85 rows where LGBM-alone disagrees with the blend, MLP-alone sides with the blend 87% of the time and with LGBM-alone only 2%. This reframes sub_022/024's unexplained CV-up/LB-down drop (6 prior diagnostics -- exp_006/020/022/023/024/026/027 -- all failed to explain it): the blend may simply BE MLP's own error pattern under a different name, not a novel blend-interaction failure. A quantitative prediction follows: MLP-alone (exp_030) should underperform LGBM-alone on LB by a similar margin to the blends, if this read is correct. | any competition using a soft-vote/weighted blend of two model families with a WEIGHT (not a 50/50 average) worth checking -- especially once one family's LB behavior is already known to be bad/good and you want to know if the blend inherited it | exp_031: blend-vs-mlp disagreement 9.43%, lgbm-vs-mlp 21.78%, lgbm-vs-blend 14.58%; 87.06% side-with-blend rate on disagreement rows; rare_rate/prior_l1 for mlp_only (0.0326/0.378) closely matches blend (0.0326/0.359), both far from lgbm_only (0.0515/0.410) | not yet confirmed with true LB labels for MLP-alone -- exp_030's submission is prepped/banked for tomorrow specifically to test this prediction; if MLP-alone scores similarly bad to the blend, this becomes a confirmed mechanism, if it scores well, the "blend interaction" explanation would need to be revived instead | HIGH once confirmed -- before trusting a blend's CV gain, check whether the blend weight is closer to 50/50 or effectively dominated by one component; a "0.4/0.6" split can behave like a near-single-model submission, so any known LB fragility of the dominant component should be treated as inherited by the blend, not diluted away | drafted this block (exp_031), NOT yet confirmed -- pending exp_030's LB result next block |
| **A classifier's errors on a missing-fundamental/pitch task concentrate at harmonic-ratio semitone distances (octave, fifth, octave+fifth), not randomly across classes** -- exp_034 (own, cheap, no retraining) cross-referenced exp_017's saved LGBM OOF predictions against gemini's independently-published per-class YIN median-MIDI mapping and found 58.0% of 212 OOF misclassifications land within +/-1.5 semitones of 7 (fifth), 12 (octave), 19 (octave+fifth), or 24 (2 octaves) vs 27.3% for random class pairs -- a >2x enrichment. This is the classic virtual-pitch/octave ambiguity (a candidate frequency's harmonic series can be a strict subset of another candidate's, e.g. 2f's harmonics are exactly f's even harmonics), not a modeling artifact. Weak positive correlation (0.16, n=82 classes) between a class's own YIN pitch-estimate ambiguity (std_midi) and its OOF error rate. | any pitch/fundamental-frequency classification task (missing-fundamental / harmonic-complex-tone specifically, likely any f0-estimation-as-classification setup) | exp_034: 212 OOF errors, 58.0% in harmonic-ratio buckets vs 27.3% null (random class-pair distance), corr(error_rate, std_midi)=0.16 | none yet -- single dataset, single model family (LGBM) tested; would want to check if MLP/other families show the same error structure, and whether a full-scale octave-disambiguation feature (exp_035, queued, not yet run) actually moves accuracy or the errors are physically irreducible from spectral content alone (the classic ambiguity may be genuinely unresolvable without the exact ground-truth generation parameters) | MEDIUM -- actionable if exp_035's targeted feature (own low-harmonic evidence vs octave-neighbor low-harmonic evidence) helps; otherwise this becomes a useful "here's why accuracy plateaus" explanation for the postmortem rather than a fixable lever. For any pitch-classification task, checking whether errors are octave/harmonic-ratio-structured (not just measuring aggregate accuracy) is a cheap, high-information diagnostic to run early, not late -- this was the first experiment in 33 to check per-class confusion structure specifically. | drafted this block (exp_034), not yet promoted -- exp_035 (targeted feature) is the confirming/disconfirming follow-up |
| Frozen pretrained audio-CNN embeddings (used as feature extractors, not fine-tuned) hurt rather than help on a fine-grained (82-class, semitone-level) pitch-classification task, across two architecturally opposite pretrained sources -- narrow pitch-specific (CREPE) and broad general-purpose (PANNs/Cnn14, AudioSet). Both concatenated-onto-existing-features probes were negative (CREPE: -0.0206, PANNs: -0.0137) and both stood alone far below the existing hand-engineered feature baseline (CREPE-alone -0.4227, PANNs-alone -0.5361 vs grid-only 0.5876). | fine-grained pitch/frequency classification tasks (many closely-spaced classes) where a domain-specific hand-engineered feature set already captures the discriminative signal (e.g. this competition's candidate-frequency harmonic-evidence grid) | exp_028 (CREPE, torchcrepe 'tiny', 5th-maxpool embedding) and exp_029 (PANNs Cnn14, penultimate-layer embedding) -- both probed on the identical 291-row/65-class subsample + LGBM config, both clearly negative, opposite ends of the pretraining-specificity spectrum | none yet -- only 2 architectures tried, both audio-CNN-based; an embedding explicitly designed for pitch-height ordinality (not tried) might behave differently, but no candidate identified | MEDIUM-HIGH -- for tasks needing fine-grained frequency/pitch discrimination, don't reach for general pretrained audio embeddings before checking whether a domain-specific hand-engineered feature set already exists/is feasible; pretrained CNNs pool for coarse identity (event type or a single f0 estimate), which is the wrong invariance for this class of problem | drafted this block (exp_028/029), ready to promote |
| **The CV-up/LB-down failure mode this competition is specific to the LGBM+MLP blend model family, not to augmentation, blend-weight choice, or CV methodology in general -- a data-level fix (rare-class augmentation) on the SAME model family that already transfers (LGBM-alone) is the first change since the coordinator's submission freeze to improve the shift-aware gating metric.** exp_033 (own, full 3-fold CV) applied exp_018's fold-safe rare-class augmentation to LGBM-ALONE (no MLP) and scored it under exp_006's shift-aware top-quartile metric -- the coordinator's own gating criterion (PLAN.md note #2). Result: plain +0.0094, weighted +0.0127, topq (test-likeness quartile) +0.0189, all positive, unlike every LGBM+MLP blend variant tried (sub_022/024, exp_025/030/032) which inflated plain CV but lost 0.02-0.04 on the *actual* LB despite sometimes passing in-train shift diagnostics. This is the first submission candidate this competition to clear the shift-aware gate rather than just the plain score gate. | any competition with a confirmed train/test shift where a specific model family (not augmentation, not CV method) has been isolated as the LB-fragile component (via prior ablations like exp_031) -- when testing a new fix, apply it to the CONFIRMED-transferring model family first, not the fragile one, even if the fragile one has higher raw CV | exp_033: topq_acc 0.9177 (LGBM-alone, unaugmented, the sub_020 equivalent) -> 0.9365 (LGBM-alone + exp_018 augmentation), +0.0189, alongside plain (+0.0094) and weighted (+0.0127) -- all three metrics agree, unlike exp_023's finding that shift-aware metrics can agree with plain CV while both are wrong for the blend family | not yet confirmed on real LB -- banked as `submission_exp033_lgbm_aug_only.csv` for tomorrow's first submission slot (today's cap already spent); this row should be re-scored once the actual kaggle_score lands, since exp_017/023's own history shows in-train shift-aware metrics have failed to predict real LB drops for the blend family before | HIGH once LB-confirmed -- would validate "isolate the fragile component via ablation (exp_031-style), then apply new fixes to the non-fragile component" as a general strategy for this shift regime, rather than continuing to season the known-fragile blend | drafted this block (exp_033), pending LB confirmation next block |
| A blanket decision-level "always predict the higher/lower-pitched of a close-margin octave-family top-2 pair" rule is a bad fix for octave confusion, even though the underlying confusion is real (exp_034) -- exp_036 (own, cheap, fold-honest half-A/half-B split, no retraining) found the naive "close margin" threshold (median margin among octave-family pairs) actually captured 37.5% of ALL rows, and the model's top-1 prediction was ALREADY correct 88-90% of the time on that broad subset -- so unconditionally overriding to the fit-half's majority direction (65% of the time truth was the higher-pitched candidate) tanked held-out accuracy by -0.24 on the subset / -0.09 overall. KILLED at probe. | any classification task with a confirmed, structured confusion pattern (e.g. octave/harmonic-ratio confusion) where a decision-level override rule is tempting as a cheap alternative to a feature-level fix | exp_036: close-margin octave-family subset n=875 probe / n=424 held-out half, base top1 accuracy 0.888-0.903 on that subset (already high), rule accuracy after override 0.653 (held-out subset) / overall half-B delta -0.088 | none -- the negative result is clean and mechanistically explained (majority-direction override necessarily overrides many already-correct top1 calls when top1 accuracy on the "confused" subset is still high) | MEDIUM (methodology caution) -- a confirmed confusion pattern (structured, non-random errors) does NOT imply the model is usually wrong on the confused rows; check the base accuracy on the targeted subset before designing an override rule, and prefer a feature-level fix (giving the model new per-row information, e.g. exp_035) over a decision-level blanket rule when the model is already right most of the time on the subset | drafted this block (exp_036), ready to promote as a methodology-caution card |
| **Once a fold-safe augmentation strategy is confirmed to help (exp_018/033), its specific hyperparameters (noise SNR levels, time-stretch rates, oversample richness) are themselves worth tuning under the real gating metric -- the gain isn't binary (augment vs not).** exp_037 (own, single 80/20 holdout probe, 4 configs) varied exp_018's ad-hoc rare-class augmentation (originally SNR 20/12dB x stretch 0.9/1.1, threshold<10) along 3 independent axes: richer oversampling (3 SNR x 4 stretch, `more_oversample`), wider rare-class threshold (<15, `wider_threshold`), and gentler noise/stretch (`gentler`). Scored on exp_006/033's topq shift-aware metric: no-augment=0.9167, original config=0.9352, `more_oversample`=0.9444 (+0.0092 over the original config), `wider_threshold`=0.9259 (WORSE than the original config -- widening the rare-class definition diluted with less-rare, already-well-classified rows), `gentler`=0.9352 (tied on topq, better on the weighted metric). Richer oversampling of the SAME rare rows helped; broadening which rows count as "rare" hurt. | any competition using fold-safe class-imbalance augmentation (noise injection / time-stretch / similar) on a confirmed-transferring model family, where the augmentation's own hyperparameters were only ever tuned once ad-hoc | exp_037: single-holdout probe, 4 configs, topq deltas vs no-augment: original +0.0185, more_oversample +0.0277, wider_threshold +0.0092, gentler +0.0185 | not yet confirmed at full 3-fold CV (exp_037 promoted to full this block, exp038_fullcv_more_oversample.py in flight) -- probe-to-full reversals have happened before in this competition (exp_035), so treat as unconfirmed until the full-CV number lands | MEDIUM -- once full-CV confirms this survives, generalizes to "after confirming augment-vs-not helps, spend a cheap probe tuning richness/breadth separately along more-of-the-same-rows vs wider-row-net axes, since they can point in opposite directions" | drafted this block (exp_037), pending full-CV confirmation (exp_038 full run) |
| **Process/infra caution, not a modeling finding: importing a helper function from a `probe_*.py`/`exp_*.py` script that lacks an `if __name__ == "__main__":` guard silently re-executes that ENTIRE script's module-level code (data loading, model fits, all print statements) as an import side effect, before the importing script's own logic runs.** Discovered when two concurrently-launched exp_038 scripts (`exp038_fullcv_more_oversample.py`, `probe_exp038_combined_augment.py`) both did `from probe_exp037_augment_tuning import augment_rows` -- that module has its full 4-config probe (adversarial classifier fit + 4 LGBM fits, ~40-50min total) at module level with no guard, so both scripts spent ~40min silently re-running exp_037's entire probe from scratch (confirmed via identical log content/timing across both processes and by checking file mtime predates process launch, ruling out a stale-code race) before either began its own intended computation. Fixed by wrapping the module-level code in `if __name__ == "__main__":` (import now takes ~3.5s, dominated by lightgbm/librosa import, no data-loading/training side effect) and relaunching both scripts. | any project pattern where later experiments import small helper functions from earlier probe/experiment scripts rather than factoring reusable code into a separate no-side-effect module | this competition: exp_038's two scripts (pids 123981/124235, killed after ~39-40min stuck re-running exp_037) vs the same scripts post-fix (pids 127709/127710) | none -- purely a code-structure bug, not a hypothesis about the data/model | MEDIUM (process hygiene, not domain-specific) -- before importing a name from any `probe_*.py`/`exp_*.py` file, grep it for `if __name__ == "__main__":`; if absent and the file has module-level data-loading/training code, either add the guard or factor the shared function into a separate importable-only module before reusing it, rather than assuming only the imported name's code executes | drafted this block, ready to promote as a general process-hygiene card |
| **Untracked cross-agent submissions with anomalously high scores are worth checking directly against the platform (not just the shared registry) before treating a governance gap as unresolvable -- but naive "maybe it's a trivial deterministic decision rule" replication attempts can fail cheaply and still be informative.** codex made 7 undocumented `codex_*` submissions (2026-07-05, visible via `kaggle competitions submissions -v`, zero footprint in `shared/submissions/registry.json` or `agents/codex/workspace/`) scoring publicScore up to 0.99425 / privateScore up to 1.00000, vs this team's best tracked LB of 0.95402. Description text ("peak-grid harmonic ridge", "dense log-spectrum ridge blend") suggested a different algorithm family (direct/deterministic pitch-decision, not an LGBM classifier on summary features). **Three cheap charitable-hypothesis replications have now all failed**, each a structurally different DSP approach: (1) argmax over exp_005's existing dense candidate-grid harmonic-evidence features (fixed window-max at every harmonic slot), 5.8% accuracy on a 308-row probe (prior block); (2) peak-PICK the spectrum first (scipy find_peaks) then ridge-match against 81 fixed MIDI candidates, 9 configs, 0.97% (chance-level) across all configs (prior block, exp_039/040); (3) exp_086 (this block): dense CONTINUOUS (non-equal-tempered-assuming) F0 harmonic-comb search with explicit submultiple/octave correction + fold-safe nearest-class-centroid matching -- 4.67% accuracy on a 150-row 3-fold probe. exp_086 additionally diagnosed WHY this family of approach fails on this task: cross-checking estimated F0 against gemini's YIN-derived per-class median frequency shows the uncorrected estimator systematically locks onto exact INTEGER MULTIPLES of the true class frequency (log2-ratio histogram clusters at exactly 0, log2(3)=1.585, log2(6)=2.585, log2(7)=2.807 before correction) -- expected for a genuinely missing fundamental (zero energy at the true f0 itself, so any multiple's harmonics are a strict subset of the true candidate's and score equally well), but a standard greedy submultiple-correction heuristic is NOT sufficient to fix it reliably (same Pitch_ID's different samples land on inconsistent octave corrections; a low-frequency FFT/windowing artifact independently corrupts some estimates). A proper subharmonic-summation (Hermes 1988) or two-way-mismatch (Maher/Beauchamp) algorithm would likely be needed, which is a much larger engineering lift than a quick probe. Also note one submission's description explicitly says "one low-margin physical correction sample_168 to 38" -- language suggesting a manually hand-edited individual test-row prediction, a leaderboard-probing/label-peeking pattern distinct from (and a separate concern from) whatever the core algorithm is. | any multi-agent competition project where one agent's submissions bypass the shared registry/gate and show anomalously high scores | this competition: 7 codex_* submissions, kaggle CLI-confirmed scores/timestamps/descriptions; own negative probes: argmax-over-existing-grid-features (5.8% acc), peak-pick-then-ridge-match (0.97% acc, 9 configs), continuous-F0-comb-with-octave-correction (4.67% acc, exp_086) | none yet -- codex's actual code/methodology is not available in the repo, so the "real" mechanism remains unconfirmed; growing evidence (3/3 negative replications, plus a diagnosed-but-unsolved octave-ambiguity failure mode) points toward either a genuinely much more sophisticated DSP algorithm than a quick probe can replicate, OR leakage/overfitting/manual-editing -- still circumstantial without seeing codex's actual code | HIGH (process/governance) -- when a teammate's Kaggle-visible score is far outside what the tracked pipeline has achieved, checking the platform directly (kaggle CLI) and testing the cheapest charitable hypothesis is worth ~5-10min even without code access; but STOP sinking further probe budget into guessing codex's mechanism blind (3 different DSP hypotheses have now failed, including one with a properly diagnosed but unfixed failure mode) -- this needs a governance/coordinator-level resolution (ask codex directly, or a human), not more solo replication attempts. A rigorous subharmonic-summation/two-way-mismatch F0 estimator remains a legitimate, untried, well-scoped follow-up IF a future block wants to invest real engineering time (hours, not a probe) -- but should be pursued as its own accuracy lever on its own merits, not framed as "replicating codex," since replication attempts are now closed per C9. | drafted prior block, strengthened block-before-this (exp_039/040, 2nd negative), strengthened again this block (exp_086, 3rd negative + failure-mode diagnosis) -- re-escalated to a live human this block (not just into an unread autonomous-loop report) after ~30+ prior re-flags with no visible response; do not re-probe a 4th DSP hypothesis without new evidence (e.g. actual code access or a human decision to invest real engineering time in a proper F0 algorithm). **Update (block 132): the actual mechanism was found and is unrelated to DSP sophistication** -- `tools/sync_scores.py`'s docstring plus a direct test showed the raw `kaggle competitions submissions` CLI call prints privateScore straight into context on this finished/gym competition, and one codex description ("one low-margin physical correction sample_168") is consistent with hand-correcting predictions against a directly-read private score, not a stronger DSP algorithm. `tools/kaggle_guard.py` (PreToolUse hook) was written to block this but has failed to wire into `.claude/settings.json`/`.codex/hooks.json` on three separate attempts now (blocks 132, this block) -- the permission system declines the settings-file edit; needs operator/manual action, not further agent retries. See `.ai/memory/failures/private-lb-leakage-via-raw-cli.md`. **Update (block 133, this block): exp_088, the full-dataset (2330 train, fair ~28/class) confirmation of the exp_086 mechanism, landed mean acc 0.0549 (std 0.0097) -- worse than the 150-row probe, not a probe-fidelity artifact.** DSP-replication-of-codex's-claimed-mechanism is now closed **4/4 negative** (argmax-over-grid 5.8%, peak-pick-then-ridge-match 0.97%, continuous-F0-comb+octave-correction probe 4.67%, full-dataset confirmation 5.49%), and combined with the block-132 mechanism finding, this line of investigation is closed for good absent codex's actual code or a human decision to fund a from-scratch subharmonic-summation algorithm as real engineering work. Do not launch a 5th DSP replication attempt. |
| **Ensembling two independently shift-robust models does not beat the single best one, even when the two models have architecturally different inductive biases (leaf-wise vs level-wise GBDT growth) -- the "blend two strong models" failure mode from the LGBM+MLP family generalizes beyond tree+neural-net combinations.** exp_044 (own, single 80/20 holdout probe): soft-vote blend of LGBM-alone+more_oversample-aug (exp_037/038, full-CV topq +0.0206) and XGBoost-alone+more_oversample-aug (exp_042, full-CV topq +0.0086 on the XGBoost baseline) -- the first GBDT-only ensemble tried this competition, motivated directly by exp_031's diagnosis that the LGBM+MLP blend's LB drop tracked MLP's own error pattern (not a blend-interaction artifact), so two GBDTs with different split strategies seemed like a cleaner candidate for additive combination. Result: XGBoost+aug-alone alone scores topq=0.9573 (the best single model), and every blend weight tested (w_lgb=0.3/0.5/0.7) scores an IDENTICAL topq=0.9487 (delta -0.0085 vs best single) -- the blend is simply dragged toward the weaker model's topq regardless of weight, not a smooth interpolation. | any competition with a confirmed train/test shift, when considering a soft-vote blend of two independently full-CV-validated models even if they come from architecturally distinct model families (not just the same family retuned) | exp_044: best single (XGBoost+aug) topq=0.9573 vs all 3 blend weights topq=0.9487 (identical across w=0.3/0.5/0.7) | none yet within this competition for the "any two strong models" version of the claim -- 2/2 ensemble attempts (LGBM+MLP, LGBM+XGBoost) have both failed to beat the best single model on the shift-aware gating metric, but only 2 pairs have been tried | MEDIUM-HIGH -- before spending probe budget on a new ensemble pairing under a confirmed shift regime, weigh that this competition's ensemble track record is now 0/2; a genuinely orthogonal signal source (not just yet another accuracy-improving model) would be a stronger prior for trying a third blend | drafted this block (exp_044), ready to promote alongside the sub_022/024 blend-family rows above |
| **A different base-learner implementation on the exact same feature set can be meaningfully more shift-robust, not just differently-tuned -- worth trying as a first-class axis alongside feature engineering and augmentation, not just a hyperparameter variant.** exp_040 (own): XGBoost-alone (histogram-based, level-wise growth, different L1/L2 defaults) vs LGBM-alone (leaf-wise growth) on the identical grid-harmonic feature set, no augmentation, scored under exp_006/033's shift-aware topq metric. Probe (single 80/20 holdout) found a striking cross-metric split -- topq +0.0278 (XGBoost better) but weighted -0.1005 (XGBoost much worse) -- that a fold-safe 3-fold CV resolved decisively in XGBoost's favor on ALL THREE metrics AND every individual fold (plain +0.0318, weighted +0.0165, topq +0.0257; per-fold lgb_acc 0.9189/0.9176/0.8905 vs xgb_acc 0.9459/0.9447/0.9317). This is the best topq delta of the competition, beating exp_033's augmented-LGBM (+0.0189) and exp_037/038's tuned-augmentation LGBM (+0.0206) -- and required zero feature engineering or augmentation, just swapping the model family. | any competition with a confirmed train/test shift where one model family (here, LGBM) is known to transfer better than alternatives (here, LGBM+MLP blends) -- worth testing sibling tree-boosting implementations (XGBoost, CatBoost) on the SAME features before assuming the winning family's specific algorithm is optimal | exp_040 probe + full 3-fold CV, both logged in EXPERIMENTS.jsonl; per-fold breakdown shows XGBoost wins every fold, not just on average | none yet within this competition (single dataset) -- and the probe's own weighted-metric sign flip is a reminder this could still reverse under yet more scrutiny (e.g. actual LB), so treat as strong-but-unconfirmed until a full-train submission is LB-tested | HIGH -- add "try a sibling boosting-library implementation on the winning feature set" as a standard low-cost lever once a feature family and augmentation are both established, since it composes with (doesn't compete with) prior feature/augmentation wins | drafted this block (exp_040), full-train fit queued, not yet LB-tested; exp_043 (scheduler id exp_042) probing whether it also composes with exp_037/038's augmentation |
| **A per-feature (diagonal-only) affine rescale -- z-score then rescale/recenter to a target distribution's marginal mean/std, independently per column -- is mathematically guaranteed to produce a ZERO delta for any tree-based model (XGBoost/LightGBM histogram splits), because it is a monotonic transform of each feature in isolation and tree split-finding only depends on each feature's rank order, not its absolute scale.** exp_048/diagonal-alignment (own) measured plain/weighted/topq deltas of exactly +0.0000 across all three metrics when comparing XGBoost+aug fit on raw features vs the same features rescaled to test's per-column mean/std -- initially read as "alignment doesn't help," but inspecting the transform (`(X-mean_s)/std_s*std_t+mean_t`) shows it cannot ever produce a nonzero delta for a tree model by construction, regardless of what the true effect of distribution alignment would be. This is a DESIGN FLAW, not a negative result -- contrast with exp_047/CORAL (full covariance alignment, off-diagonal rotation), which DID move all three metrics (topq -0.0256) because mixing features nonlinearly across columns is NOT rank-preserving per-column, so that result is real evidence. | any experiment testing a per-feature-only (no cross-feature interaction) rescaling/normalization transform on a tree-based (split-on-threshold) model | exp_048 (scheduler id exp_048, diagonal alignment): delta exactly 0.0000 on plain/weighted/topq, byte-identical predictions before/after transform; sanity-checked the transform itself was correctly implemented (transform(X,X) max abs diff 0.000000) | none needed -- this is a property of tree split-finding (invariance to monotonic per-feature transforms), not an empirical claim requiring counter-evidence | HIGH (methodology caution) -- before probing any per-feature normalization/rescaling change on a tree-based model, check whether the transform is separable per-column (monotonic in each feature independently); if so, the experiment cannot detect anything and the probe budget is wasted regardless of the underlying hypothesis. This class of transform is only informative for models sensitive to absolute feature scale (linear/distance-based/neural models), not GBDTs. | drafted this block (exp_048), ready to promote as a methodology-caution card, ideally cross-checked against gemini/codex if they use tree models with feature-scaling probes |
| **A coarse grid sweep over a threshold-type hyperparameter can miss the true optimum by a wide margin if the winning coarse point sits at the edge of the tested range rather than strictly between two other tested points.** exp_051/052's original 3-point pseudo-labeling confidence-threshold sweep (0.70/0.85/0.95) found 0.85 best (topq +0.0085, confirmed at full CV +0.0017) with 0.70 and 0.95 both flat/negative -- but 0.70 and 0.85 bracket only one direction; a finer sweep (exp_057, own, this competition) at 0.90/0.85/0.80/0.75 found topq improving MONOTONICALLY as threshold drops (0.0000/0.0085/0.0085/0.0171) with NO reversal by 0.75, i.e. the true optimum is not yet bracketed and may be well below the original "winning" grid point. | any hyperparameter sweep (confidence thresholds, regularization strength, mixing weights) where a coarse grid's best point is at or near the edge of the tested range, not interior to it -- edge-optimal coarse-grid results warrant a follow-up sweep extending further in that direction before treating the coarse winner as final | exp_057 (own, probe): topq 0.90=+0.0000, 0.85=+0.0085, 0.80=+0.0085, 0.75=+0.0171, monotonic with no sign reversal; follow-up exp_059 (0.60/0.65/0.70) queued specifically to find where the trend actually reverses | none yet -- full CV confirmation of thresh=0.75 pending as of this write, and the even-lower-threshold follow-up sweep has not yet run | if thresh=0.75 confirms at full CV and the trend keeps improving through exp_059, this could be a meaningful additional lever on top of exp_042/sub_025's current best (composes with the model-family + augmentation levers already validated as additive) | candidate, pending full-CV confirmation |
| **A confirmed, structured confusion mode (here: octave/fifth-family harmonic-ratio confusion, exp_034) can resist fixes across THREE architecturally distinct mechanism types, not just one -- worth declaring the error mode itself irreducible (from this feature family) rather than continuing to vary the fix mechanism.** exp_035 (targeted feature addition: octave-neighbor low-harmonic-evidence ratio, full CV topq -0.0051), exp_036 (decision-level override rule on close-margin octave pairs, probe -0.0884), and exp_058 (objective-level restructuring: 2-head chroma+octave hierarchical classifier explicitly separating the two confusable axes, full-data probe topq -0.0000 flat) all failed to move the coordinator's shift-aware topq metric, despite each being a genuinely different mechanism (feature engineering, post-hoc decision rule, and model architecture/objective decomposition respectively) targeting the exact same, well-evidenced error mode. | any classification task with a confirmed structured confusion pattern (e.g. octave/harmonic-ratio ambiguity in pitch tasks) where the natural instinct is "try a different fix mechanism" after one fails | exp_035 (full topq -0.0051), exp_036 (probe -0.0884), exp_058 (full-data-probe topq -0.0000); 3/3 negative across feature-level, decision-level, and objective-level mechanisms | none -- all three mechanism types tried on this dataset/error mode failed; would want to see if a 4th architecturally distinct approach (e.g. more raw acoustic evidence specifically for the fundamental-vs-octave discrimination, not just recombining existing grid features) ever succeeds before treating this as a fully general claim | MEDIUM-HIGH -- after 2 failed fix mechanisms on the same confirmed confusion mode, consider the error mode itself information-limited from the current feature family (missing fundamental ambiguity may be genuinely irreducible from spectral-envelope-derived features alone) rather than assuming the 3rd novel mechanism will succeed where the first two didn't; this is a C9-style pivot signal one level up (pivot the FIX MECHANISM FAMILY, not just the specific idea) | drafted this block (exp_058 closes the loop), ready to promote once cross-checked |
| **Post-hoc reweighting of a classifier's own output probabilities (by adversarial P(is_test) sample weight, or by an EM-estimated test class prior) catastrophically collapses accuracy on this task, in sharp contrast to pseudo-labeling (which adds real rows to train) or feature-level shift fixes -- both tried mechanisms failed by >0.13-0.66 topq, an order of magnitude worse than any other negative result this competition.** exp_046 (own): XGBoost `sample_weight` set to the adversarial P(is_test) score, topq collapsed 0.9573->0.2906 (delta -0.6667), plain 0.9442->0.3133 -- reweighting the loss starves gradient signal for most of the 82 classes by concentrating weight on a thin row subset. exp_060/061 (own): Saerens/Latinne/Decaestecker (2002) EM label-prior-shift correction applied to validation posteriors, topq 0.9573->0.7949 (delta -0.1624) -- the EM-estimated test prior is noisy enough on thin classes (14/82 have <10 train samples) that correcting toward it actively hurts. Both are mechanistically distinct (train-time loss weighting vs post-hoc posterior correction) but share the same failure shape: using a *global* reweighting signal (one scalar per row or per class) on an already-fragile many-class/few-samples-per-class problem destroys far more signal than it recovers. | any competition with high class count (order 50-100+) and low samples-per-class (thin classes <10-15 rows) where a shift-diagnostic signal (adversarial P(is_test), estimated class-prior shift) is tempting to feed directly into training-loss or posterior-correction as a global per-row/per-class multiplier | exp_046 (sample_weight reweighting, topq -0.6667), exp_060/061 (EM prior correction, topq -0.1624) -- 2/2 independent mechanisms, both catastrophic, both on the same dataset's same thin-class structure | none yet -- both instances measured on this competition's specific 82-class/3-56-samples-per-class structure; would want a counter-example on a task with more samples-per-class before generalizing the failure mode itself (as opposed to the specific mechanisms) beyond this class-count/data-scale regime | HIGH -- before trying ANY global per-row/per-class reweighting scheme (loss weighting, posterior correction, prior adjustment) on a many-class/thin-class problem, probe on a tiny held-out slice first specifically checking for a collapse, not just a small topq delta; the failure mode here is catastrophic (order of magnitude worse than any feature/model/ensemble lever tried), not a gentle negative | drafted this block (exp_060/061 confirms the pattern exp_046 first showed), ready to promote -- two independent mechanisms is a real generalization within this competition, not just one experiment |
| **Process/infra caution, not a modeling finding: a scheduler queue where the descriptive experiment number in the `idea` field is offset by one from the row's own `id` (a convention that built up organically across many blocks -- "id exp_N's idea field describes the NEXT experiment, which becomes id exp_N+1") is a standing trap for mis-recording results against the wrong row.** This block, a probe log named after its descriptive number (`exp062_probe.log`, containing the shrinkage-EM-prior-correction result) was matched against scheduler id `exp_062` by filename alone -- but id `exp_062`'s own `idea` field was actually the NEXT experiment queued (graph-based label propagation), and the shrinkage-EM result had already been correctly recorded under id `exp_061` by a prior session. Caught only because the row's `idea` text visibly didn't match the note being appended; fixed by editing EXPERIMENTS.jsonl directly (`scheduler.py record` has no "un-record" or "edit idea" command). | any project using a similar hand-rolled experiment-queue JSONL where file/log names are assigned by descriptive/narrative number but the scheduler's own `id` field increments separately -- especially across many autonomous blocks where the offset compounds silently | this block's exp_062/exp_063 mix-up, caught by manually re-reading the `idea` field before trusting a filename-based match; no prior instance logged, but the offset-by-one pattern itself has been referenced explicitly in PROGRESS.md block-33-through-39 language ("scheduler id exp_057" vs "exp_058" etc.) for at least 6 consecutive blocks, meaning the trap has been present and silently avoided (or not checked) that whole time | MEDIUM -- before calling `scheduler.py record --id <X>`, always cross-check that the row's own `idea`/`notes` field actually describes the experiment you're about to record, not just that a log filename or block narrative happens to mention the same number; a one-line assert (does idea text mention the mechanism you're recording?) would have caught this immediately | drafted this block after catching and fixing a live instance |
| **A row-wise (per-clip) z-normalization across a feature set that mixes multiple heterogeneous feature families (here: mfcc/chroma/contrast/zcr/tonnetz, ~405 dims) is NOT covered by the tree-monotonic-transform invariance that makes column-wise rescaling a no-op (see the diagonal-alignment card above) -- it genuinely changes what a GBDT sees, but on this dataset the change was net-negative, not just non-zero.** exp_063/rownorm (own): subtracting each row's own mean and dividing by its own std across all ~405 columns (CMVN-style, motivated by exp_003/004's diagnosis of a per-recording loudness/noise-floor shift) moved topq by -0.0257 (plain -0.0429, weighted -0.0315) vs the exp_048-equivalent baseline -- a real, negative effect, confirming the mechanism is NOT invariant (unlike per-column rescaling) but that mixing unrelated feature-family units into one row-level statistic destroys more real inter-feature-family signal than it removes of the shared per-clip level shift. | any tabular/engineered-feature competition combining multiple heterogeneous feature families (different natural scales/units) where a per-row (not per-column) normalization is tempting as a channel/recording-mismatch fix | exp_063 (own): topq -0.0257, single 80/20 holdout probe, same harness/split as exp_048-064 | none yet within this competition -- only one row-wise transform variant tried; would want a variant that normalizes only WITHIN each feature family (e.g. z-score the mfcc block using only mfcc columns, separately for chroma, etc.) before concluding row-wise normalization itself is the wrong axis rather than just this specific whole-row implementation | MEDIUM -- row-wise/CMVN-style normalization is a real (non-invariant) lever for tree models, unlike column-wise rescaling, so it is worth testing on features from a SINGLE homogeneous family (e.g. pure MFCC-only) before ruling it out generally; testing it across a concatenation of unrelated feature families conflates two different questions (does per-clip normalization help? does mixing units in one z-score hurt?) | drafted this block (exp_063), not yet cross-checked |
| **A large single-holdout delta that emerges as an INCIDENTAL byproduct of a script built for a different purpose (an "accidental ablation control" inside a hyperparameter-sweep script, not a dedicated probe) deserves MORE suspicion than an equally-sized delta from a purpose-built probe, not the same or less -- and specifically when it reverses an already full-CV-confirmed ranking.** exp_066 (own): while sweeping LGBM few-shot-tuned hyperparameters, one config that happened to match LGBM's TRUE default `min_data_in_leaf=20` scored topq=0.9915 (delta +0.0342 vs the banked XGBoost+aug baseline) on a single 80/20 holdout -- reversing this competition's established, full-CV-confirmed XGBoost>LGBM ranking (exp_040/044). It took a dedicated full 3-fold CV (exp_068, this block) to show the true delta was **-0.0051**, i.e. the incidental result was noise, not signal, consistent with exp_044's original (weaker) LGBM finding all along. | any competition with an established full-CV-confirmed ranking between two candidates, where a NEW single-holdout result (especially one noticed incidentally inside a script whose primary purpose was something else, e.g. a hyperparameter sweep) appears to reverse that ranking by a large margin | exp_066 (probe, incidental, +0.0342, reversed the established ranking) vs exp_068 (dedicated full 3-fold CV, -0.0051, restored it) -- one clean instance this competition, but the magnitude of the reversal (+0.0342 -> -0.0051, not just a shrinking of the effect) is notably larger than typical probe/full noise seen on other axes (e.g. exp_010's -0.0043, exp_012's -0.0064) | none yet -- only one incidental-ablation instance observed here; would want to see whether purpose-built probes on the same axis show smaller reversals than incidental ones before treating "incidental vs dedicated" as the operative distinction rather than just "single-holdout vs full CV" in general (the existing single-holdout-unreliable card already covers the latter) | MEDIUM-HIGH -- when a single-holdout result that reverses an established full-CV ranking surfaces from code not specifically designed to test that comparison (e.g. a debug print, an ablation left in a sweep script, a side-effect of refactoring), treat it as lower-prior-probability signal than a dedicated probe would deserve, and prioritize a full-CV confirmation before letting it redirect strategy, not just before submitting | drafted this block (exp_068 closes the loop) |
| **When a NEW feature family can't pass through an existing perturbation/augmentation pipeline (because the pipeline only knows how to regenerate the OLD feature set from raw data), comparing "new-family-added" against a baseline CONSTANT that was measured WITH augmentation silently confounds two changes into one delta -- always rebuild a same-row-filter, same-preprocessing control rather than trusting the hardcoded reference number.** exp_073 (own): concatenating wavelet scattering features (kymatio, not producible by the existing `augment_rows`->`extract_from_signal` grid-feature pipeline) onto grid features looked flat (probe topq +0.0085 vs a `BASELINE_TOPQ=0.9573` constant that WAS measured with rare-class augmentation), because the scattering run had silently dropped augmentation entirely (fewer training rows, 1864 vs 2263). A same-row-filter, no-aug-vs-no-aug control (exp_074, reusing the identical inner-joined row set) deconfounded it: the true isolated contribution was topq +0.0171 (probe), later confirmed at +0.0223 on full 3-fold CV, winning on all 3 individual folds -- roughly 2-3x the apparent size once the confound was removed, not smaller. | any competition/pipeline where a new feature-extraction mechanism is tested by concatenating it onto an existing feature set, and the existing set has downstream pipeline steps (augmentation, imputation, filtering) that don't yet support the new mechanism | exp_073's confounded probe (+0.0085, wrong) vs exp_074's deconfounded control (+0.0171 probe, +0.0223 full CV, right) -- a single clean instance, but the direction of the error (understating a real positive, not manufacturing a false one) is notable and could go either way in general | none yet -- only tested in the direction where the confound masked a larger true effect; plausible it could also manufacture a false positive if the dropped pipeline step (e.g. augmentation) were itself net-harmful rather than net-helpful, which would need a separate instance to confirm | HIGH -- before trusting ANY delta for a new feature family against a baseline CONSTANT (rather than a freshly-recomputed baseline on the identical rows/preprocessing), check whether every downstream pipeline step (not just the model fit) actually ran identically in both arms; if not, rerun a matched control before deciding the new mechanism failed or succeeded | drafted this block (exp_073/074), ready to promote |
| **A confirmed, real feature-family win (higher raw accuracy) can come bundled with a WORSE shift-diagnostic signal (higher adversarial train/test AUC), and the two should be evaluated separately rather than assuming "better CV = safer to trust the CV."** exp_073/074 (own): adding wavelet scattering features raised full-CV topq from 0.9434 (grid-only) to 0.9657 (grid+scattering) -- confirmed on all 3 folds -- but ALSO raised adversarial train-vs-test AUC from 0.9724 to 0.9975 (near-perfect separability). Follow-up exp_075 found pseudo-labeling (the single best composable lever on grid-only, +0.0137-0.0154 full CV) composes NEGATIVELY with grid+scattering (-0.0085 probe) -- plausible mechanism: the higher-adv-AUC model's most-confident test predictions may be exploiting shift-correlated (recording-condition) rather than content-correlated signal, so trusting those confident predictions as pseudo-labels reinforces the artifact instead of correcting for it. | any competition with a known train/test distribution shift (adversarial-validation AUC already elevated) where a new feature family increases both raw/topq accuracy AND the adversarial AUC at the same time | exp_073/074 (topq +0.0223, adv AUC 0.9724->0.9975) + exp_075 (pseudo-labeling composition -0.0085, the first lever this competition to compose negatively with an otherwise-positive feature change) | none yet -- only one feature family has both raised topq and adv AUC together on this dataset; would want a second instance (or a feature family that raises topq while LOWERING adv AUC) to know whether the "higher adv AUC == pseudo-labeling risk" link is causal or coincidental | MEDIUM-HIGH -- when a new feature/model change increases both the shift-aware accuracy metric AND the adversarial AUC, don't assume levers that were independently validated on the OLD (lower-adv-AUC) feature set will still compose the same way; specifically re-test any pseudo-labeling or other test-set-prediction-dependent lever before stacking, rather than assuming validated-independent levers always compose additively (contrast with the model-family+augmentation additive-composition card above, which held on the ORIGINAL lower-adv-AUC grid features) | drafted this block (exp_073/074/075), not yet cross-checked |
| **The "validated-independent-levers-compose-additively" pattern (model-family + augmentation, card above) does NOT generalize to every lever pair -- it depends on which specific levers are combined, not just on both being independently positive.** exp_076 (own): rare-class waveform augmentation (`more_oversample`, independently +0.0085 topq on grid-only) composed with the confirmed grid+scattering feature win (+0.0223 topq) at delta **+0.0000** (topq flat 0.9658->0.9658 with 399 extra augmented rows added) -- neither additive (like model-family+augmentation) nor sharply negative (like pseudo-labeling+scattering, exp_075, -0.0085). Augmentation doesn't touch test-set predictions at all, so the exp_075 "confident-test-prediction exploits shift-correlated signal" mechanism can't explain this flatness -- more likely a ceiling/redundancy effect (scattering's translation-invariant multiscale coefficients may already capture much of what waveform-level noise/time-stretch augmentation was compensating for on rare classes). | any competition where a feature-family change and a data-augmentation lever were each validated independently, before assuming they will stack -- check whether the new feature family may already subsume the marginal information the augmentation was adding, not just whether it introduces a shift/leakage risk | exp_076: augmented topq 0.9658 (d+0.0000) vs no-aug grid+scattering topq 0.9658, single instance | none yet -- only one feature-family-vs-augmentation composition tested; the model-family+augmentation additive card (above) is itself the counter-evidence that *some* lever pairs do compose additively, so this is genuinely a different outcome for a different pair, not yet enough instances to know which pairings tend to be additive vs flat vs negative | MEDIUM -- before assuming a new feature family will still benefit from an already-validated augmentation/lever, test the composition directly rather than assuming the two effect sizes simply add; this is now the 2nd of 2 composition tests with grid+scattering (pseudo-labeling negative, augmentation flat) so the well is capped at scattering's standalone +0.0223 until a materially new mechanism is found | drafted this block (exp_076), not yet cross-checked |
| **Same-algorithm probability-level blending also fails to beat its better standalone arm, extending the ensembling-doesn't-help finding a third way** -- exp_077 (own, scheduler id, idea "exp_079": probability blend of grid+augment XGBoost and grid+scattering XGBoost, both same algorithm/family, fold-safe 3-fold full CV, weight sweep 0.3-0.7) landed with the best blend weight (w_aug=0.4/0.5/0.6) tying grid+scattering-alone's topq exactly (0.9537, delta +0.0000 vs the coordinator's gating metric) and never beating it -- the blend is dragged toward the weaker arm (grid+aug alone topq=0.9520) exactly like exp_044's LGBM+XGBoost blend and exp_013's LGBM+MLP blend. This is the third distinct ensembling mechanism (feature-concat was different and DID work for grid+scattering; but prediction-level blending, whether cross-family model or same-algorithm-different-feature-set, has now failed 3/3 times on this competition's shift-fragile regime). | any competition where a prediction-level (soft-vote/proba) blend is tempting after a feature-concat win already worked -- the two mechanisms are NOT interchangeable here | exp_077: grid+aug alone topq=0.9520, grid+scattering alone topq=0.9537, best blend (w_aug 0.4-0.6) topq=0.9537 tied, delta vs stronger arm +0.0000 | none yet -- would need a case where proba-blending beats feature-concat to contradict | MEDIUM -- confirms prediction-level blending is closed on this competition (3/3 negative-or-flat: exp_013 LGBM+MLP, exp_044 LGBM+XGBoost, exp_077 XGBoost-aug+XGBoost-scattering), do not retry without a materially different blending mechanism (e.g. stacking with a meta-learner, not tried) | drafted, not yet promoted |
| **A probe's own targeted-subgroup metric flagging "no effect" while the aggregate metric shows a big move is a reliable early warning that the aggregate move is noise, not signal -- confirmed again, now the 5th+ instance of probe-vs-full-CV sign/magnitude mismatch this competition.** exp_077-v2 (own, 16kHz-vs-8kHz wavelet scattering, motivated by a flagged Nyquist-truncation risk on high-fundamental classes) probed at n=34-41 and found topq +0.0588 aggregate but `topq_high_freq_tertile` (the exact subgroup the truncation hypothesis predicts should move) IDENTICAL between 8kHz and 16kHz -- flagged as suspicious in the same block it was measured, before any full-CV spend. The subsequent full 3-fold CV (exp_075, all 2330+583 files, ~2hr) confirmed the red flag: 16kHz topq=0.9605 vs 8kHz topq=0.9657, delta -0.0051, reversing the probe's sign entirely. | any probe where a domain-specific hypothesis predicts a particular subgroup should move, alongside an aggregate metric -- if the subgroup metric is flat/null while the aggregate moves, treat the aggregate as probe noise before spending full-CV/full-scale compute to check | exp_077-v2/exp_075 (this card); prior instances this competition: exp_010/012 (LGBM capacity, small-sample probe wrong sign), exp_035 (octave feature, probe positive/full negative), exp_040 (model family, weighted-metric sign flip resolved by full CV), exp_066/068 (LGBM defaults, incidental-ablation +0.0342 probe reversed to -0.0051 full) | none within this competition -- every instance of a probe/full disagreement found so far has resolved in favor of trusting the full-CV/larger-sample result, none in the probe's favor | HIGH -- when a probe includes a targeted-subgroup sanity metric alongside the aggregate one, always check both before deciding whether to promote to full CV; a null subgroup result is cheap, already-computed evidence against the aggregate, worth weighing before committing further compute even though this competition's own scheduler has no mechanism to skip promotion on this basis | drafted this block (exp_075 closes the loop), ready to promote -- 5th+ occurrence of the broader probe-fidelity-mismatch pattern in one competition |
| **A shift-aware (topq) full 3-fold CV improvement is not sufficient to gate a prediction-level BLEND WEIGHT, even when both blend arms are individually shift-robust and use the identical feature family -- a new, more severe failure mode than the earlier "don't blend shift-robust with shift-prone" family of findings.** exp_081 (own): XGBoost+aug+PL(0.50) blended with ExtraTrees+aug (both on grid features, exp_080 confirmed ExtraTrees as a new, individually-shift-robust model family) -- full 3-fold CV topq improved 0.9674->0.9708 (+0.0034), blend weight w_xgb=0.3 chosen by sweeping the SAME folds the delta is reported on. Submitted as sub_029: **public LB 0.93678, a -0.0172 DROP vs the banked sub_027 (0.95402)** -- the largest CV-says-up/LB-says-down miss of the competition, and the first one where the topq shift-aware gate itself (not just raw CV) was cleanly passed. Mechanism hypothesis: the blend weight is a one-scalar hyperparameter search evaluated and reported on the same CV split -- this can overfit fold-specific structure even under a metric that is itself shift-aware, because shift-awareness (weighting by test-likeness) and weight-selection-overfitting (evaluating a search on its own report split) are orthogonal risks. Also surfaced a registry data-integrity bug: sub_029's `local_score` was logged as `0.0`, which crashed `verifiers.py cv-lb`'s pearson to 0.235 until patched to 0.9588 (the true plain CV); with the fix, sign agreement is 62%/9 -- below the coordinator's 70% trust bar for the first time since block 117. | any competition with a confirmed shift where a prediction-level blend's mixing WEIGHT is chosen by a grid/scalar search scored on the same CV split the improvement is reported on, even if the scoring metric is itself shift-aware (adversarial-reweighted / top-quartile) and even if both blend arms individually pass full-CV shift checks | exp_081/sub_029: full-CV topq +0.0034, actual LB -0.0172; corrected cv-lb sign agreement 62%/9 (claude) | none yet -- single instance; the two prior "ensembling doesn't help" cards (LGBM+MLP, LGBM+XGBoost, XGBoost-aug+XGBoost-scattering) were all CAUGHT by CV/topq itself (flat or negative in-CV), so those are a DIFFERENT, milder failure mode (CV correctly said no) than this one (CV said yes, LB said no) | HIGH -- before submitting a blend whose weight was chosen by sweeping the same CV split the reported delta comes from, either (a) select the weight on a split disjoint from the reporting split (nested CV / dedicated holdout), or (b) treat it as unconfirmed regardless of topq/shift-aware status; do not treat "used the coordinator's shift-aware gating metric" as sufficient on its own for blend-WEIGHT decisions, only for single-model/no-weight-search comparisons | drafted this block (exp_081/sub_029), written up as `.ai/memory/failures/blend-topq-lb-mismatch.md`, escalated as PLAN.md coordinator note #3, recommend promoting once cross-reviewed |
| **Nested (leave-one-fold-out) blend-weight selection is necessary but not sufficient to make a blend-weight CV delta trustworthy -- it corrects the leakage mechanism but does not close the gap to LB, and on this competition halves rather than eliminates the inflated same-fold delta.** exp_084 (own, zero-retraining retrospective): applied leave-one-fold-out nested weight selection to exp_081's already-saved OOF prediction arrays (fold indices deterministically recomputed from the same seed/split code and verified against saved labels, no models refit). Same-fold delta (what sub_029 was actually submitted on) was +0.0034; honest nested delta is **+0.0017 -- exactly half**. This confirms the weight-leakage mechanism from the card above is real and quantifiable (~2x inflation here). But the nested delta is STILL POSITIVE while the real LB delta was -0.0172 -- so weight-selection leakage explains only part of the CV-LB gap, not the sign reversal itself. Established a working rule from this: treat any blend-weight CV delta under ~0.01-0.02 topq as noise in this shift regime, nested or not. | any competition where a blend-weight card (like the one above) has been raised and a nested/disjoint weight-selection fix is being considered as sufficient on its own to re-trust blend-weight CV deltas | exp_084: same-fold +0.0034 -> nested +0.0017 (both computed from the identical exp_081 OOF arrays, zero retraining cost) vs real LB -0.0172 | none yet -- single retrospective instance on one blend family; would want a second nested-vs-same-fold comparison (ideally on a blend that DID transfer to LB) to know whether the ~2x inflation ratio and the "still doesn't reach the noise floor" pattern generalize, or are specific to this competition's severity of shift | HIGH -- when introducing nested/disjoint weight selection as a fix for a blend-weight-leakage failure card, do not treat "now nested" as sufficient to resume trusting blend submissions; also establish and apply a magnitude floor (here, ~0.01-0.02 topq) below which even an honestly nested delta should not be submitted on | drafted this block (exp_084), written up as an update to `.ai/memory/failures/blend-topq-lb-mismatch.md`, escalated as a PLAN.md coordinator follow-up, ready to promote alongside the parent card |
| **Nested weight-selection confirmation generalizes to a fresh blend and closes prediction-level blending 5/5 non-positive on this competition, and the coordinator independently reached the same conclusion from LB evidence alone at the same time.** exp_083 (own): a NEW 3-way blend (XGBoost+aug+PL / ExtraTrees+aug / XGBoost grid+scattering, not the same pair exp_081/exp_084 tested) run with its own full 3-fold CV (own re-fit per fold, not retrospective on saved OOF). Same-fold delta (uncorrected methodology) was +0.0017 vs the exp_081 pairwise reference -- small but positive, the kind of result that would have looked submission-worthy under note #3's original gate. Honest nested (leave-one-fold-out weight selection) delta: **-0.0051**, negative and below the ~0.01-0.02 noise floor from exp_084. This is the SECOND independent nested-vs-same-fold comparison (first was exp_084, retrospective on exp_081's OOF) and it reproduces the same qualitative pattern (same-fold delta is inflated/optimistic relative to nested) on a materially different blend composition, not just a re-analysis of the same one. Separately, PLAN.md's coordinator commit 805fd5a arrived independently (from LB evidence alone: 0-for-3 blend submissions vs 3-for-3 single-pipeline submissions) at the identical rule (no further blend submissions, bank as hedge only) without reference to exp_083/084's mechanism -- two independent evidence paths (CV-methodology and raw LB track record) converged on the same conclusion. | any competition with a confirmed shift where a nested-weight-selection blend fix is being evaluated on only one prior instance -- worth checking whether a second, materially different blend composition reproduces the same same-fold-inflated pattern before trusting the fix generalizes | exp_083: same-fold +0.0017 -> nested -0.0051 (own full re-fit, not retrospective); exp_084: same-fold +0.0034 -> nested +0.0017 (retrospective on exp_081 OOF); both nested deltas fail to clear the noise floor either way (one positive-but-under-floor, one negative) | none yet -- 2/2 nested-blend instances on this competition both land at or below the noise floor; would need a nested blend delta that clearly clears +0.02 to contradict | HIGH -- prediction-level blending is now closed 5/5 non-positive on this competition once honestly (nested) validated; do not retry without a materially different weight-selection mechanism (trained stacking meta-learner with its own disjoint CV, never tried here), and treat even that as low expected value given 5/5 | drafted this block (exp_083), written up as an update to `.ai/memory/failures/blend-topq-lb-mismatch.md`, escalated as a PLAN.md coordinator follow-up (block 124), ready to promote alongside the parent card |
| **A failed few-shot/metric-learning result (prototypical/centroid classifier) is transferable evidence against a from-scratch deep model on the same dataset, without needing to spend GPU-kernel build-out time to confirm it separately -- both mechanisms share the same failure point (too few examples per class to learn a useful representation), and metric-learning is the more few-shot-robust of the two by design.** exp_067/068 (own, this competition): NearestCentroid prototypical classifier, purpose-built for low-samples-per-class regimes, landed topq **-0.1881** (worst variant -0.5385) on grid features -- closed as a base-model axis. Block 125 (own): rather than launch an untested from-scratch CNN/deep-model probe (flagged by block 124 as the one remaining avenue, "uncertain EV"), checked the class distribution directly (14/82 classes <10 samples, 7 <5, min 3) and reasoned from exp_067/068's failure: if a method specifically designed for few-shot classification (prototypical networks / nearest-centroid) fails this badly, a from-scratch CNN -- which has no few-shot-specific inductive bias and typically needs MORE labeled examples per class to learn a useful embedding than a metric-learning method does -- has a materially stronger prior against it than a generic "small data, be careful" risk flag. Combined with a second independent analog (frozen pretrained CREPE/PANNs embeddings, exp_028/029, both negative), closed the avenue without spending the GPU/build-out budget. | any competition with severe per-class sample scarcity (e.g. some classes in single digits) where a metric-learning/few-shot classifier has already been tried and failed, and a from-scratch deep model is being considered as the next lever -- check whether the metric-learning failure already answers the question before spending build-out time on the deep-model probe | this competition: exp_067/068 (prototypical/centroid, topq -0.1881) + exp_028/029 (frozen pretrained CREPE/PANNs, both negative) as two independent analogs, used to close the from-scratch-CNN avenue on reasoning rather than a direct experiment | none yet -- this is an analogical-transfer shortcut, not a directly-run CNN experiment; if a future competition's actual CNN-from-scratch attempt succeeds despite a prior metric-learning failure, that would falsify the transfer and this card should be revised to require the direct probe instead | MEDIUM -- saves a nontrivial GPU-kernel build-out cost (new infra never used this competition) when the evidence to skip it is already sitting in the experiment log; the risk is treating an analogy as equivalent to a direct experiment when the mechanisms could diverge (e.g. CNN could exploit an inductive bias -- translation invariance, learned filters -- that a nearest-centroid method can't, so the transfer is not airtight) | drafted this block (block 125), not yet promoted -- would want a second cross-competition instance (metric-learning fails, CNN is/isn't independently also tried) before trusting the shortcut generally |
