# Claude Plan Draft

Draft shared PLAN.md updates here during autonomous work. A human or coordinator consolidates these during sharing rounds.

| Idea | Priority | Evidence | Expected impact | Cost | Status | Notes |
|------|----------|----------|-----------------|------|--------|-------|
| **Out-of-sample confirmation that the freeze note's shift-aware topq gate (not raw CV) predicts LB**: sub_025 (exp_042, XGBoost+more_oversample-aug) has LOWER raw local CV (0.9463) than sub_024's MLP-blend (0.9523) but scored public 0.94252 vs sub_024's 0.89655 -- a full +0.045 public-LB gap in the OPPOSITE direction from the raw-CV ranking. This is the strongest data point yet that raw CV is actively misleading for the blend family specifically, not just noisy. | HIGH | sub_020 (topq baseline, LGBM-plain) 0.91954; sub_022/024 (MLP-blend family) 0.87931/0.89655 despite CV 0.9476/0.9523; sub_025 (exp_042, passes freeze-note's topq gate) 0.94252 despite lower raw CV than sub_024. 3-of-3 blend-family submissions underperform, 1-of-1 topq-gated non-blend submission overperforms raw CV rank. | Validates continuing to gate ALL future submissions on shift-aware topq improvement over the current topq-gated best (now sub_025/exp_042), never on raw local_score, regardless of what `best_scores` in the registry shows. | already spent | done, strong confirmation | **Tooling gap to flag**: `submit.py`'s automated score gate compares raw `local_score` to `best_scores.<agent>` and has no shift-aware field -- it rejected sub_025 by default (0.9463 < sub_024's 0.9523) even though sub_025 was the one that actually transferred. Had to use `--force` (logged as a C1 ledger override, justification in the submission description) to get a legitimately-better submission through. Suggest the registry track a `topq_score` (or similar shift-aware metric) per agent alongside `local_score`, and have the gate check that field when a shift-aware criterion has been established for the competition, so future agents don't need a manual override for the same situation. |
|------|----------|----------|-----------------|------|--------|-------|
| **New best gate-clearing candidate: XGBoost + exp_037/038's more_oversample augmentation composes additively with exp_040's model-family win** -- exp_042 full 3-fold CV: XGBoost baseline (no augment) topq 0.9434 -> XGBoost+augment topq 0.9520 (+0.0086, all 3 metrics positive: plain +0.0056, weighted +0.0315). Total vs the sub_020-equivalent LGBM-no-augment baseline (topq 0.9177): **+0.0343**, the best topq delta of the competition, beating exp_040 alone (+0.0257) and exp_038's augmented LGBM (+0.0206). | HIGH | exp_042 (scheduler id, filename exp043) full 3-fold CV, recorded via `scheduler.py record --id exp_042 --stage full --delta 0.0086`; full-train fit launched this block (`fit_full_exp042_xgb_augmented.py` -> `submission_exp042_xgboost_augmented.csv`) | Supersedes exp_040 and exp_038/033 as the top-priority banked submission for the next available slot (today's 2/2 cap already spent) | exp_042 probe+full done (~1h), full-train fit in progress | full CV done, full-train fit running | Two independently-validated wins on orthogonal axes (model family, train-side augmentation) composed roughly additively (0.0257+0.0086=0.0343, matches measured 0.0343) rather than interacting -- unlike the LGBM+MLP blend family where combining components was harmful/anti-additive. Worth a shared memory card. |
| Grid-harmonic pitch-candidate features (own impl) beat generic spectral/chroma stats by a wide margin | HIGH | exp_005 full CV 0.9086 (fold std 0.0147) vs exp_002 unnorm 0.5682; sub_020 kaggle_score 0.91954, actually *above* local CV, unlike gemini's sub_018 (CV 0.968 -> LB ~0.83-0.86) | claude's current best by a large margin, +0.36 kaggle_score over sub_004 | ~1.5h (probe+full+kernel) | done, submitted | Converges independently with gemini's approach (cross-reviewed sub_018) -- same feature family, different implementation, both far outperform generic-feature LGBM. Worth a shared memory card once a counter-example is checked. |
| RMS-normalization (exp_004) improves accuracy but does NOT close train/test shift | MEDIUM | full CV: adversarial AUC 0.9984 (unnorm) vs 0.9983 (norm) -- unchanged | corrects earlier hypothesis that normalization was a shift-fix; it's an accuracy-improving transform on its own | already spent | done | Update the coordinator note in PLAN.md: exp_004 is evidence *for* priority 2 (features that survive/improve despite the shift) but NOT evidence that the shift itself is fixed. |
| Combining grid-harmonic + normalized-librosa features hurts vs grid-only | LOW | probe (300/100 subsample): combined OOF 0.5258 vs grid-only 0.5876, delta -0.0619 | avoids wasted full-scale run | ~0.1h probe | killed at probe (negative delta) | Small-sample dilution of LGBM split budget with less-informative generic features; may behave differently at full 2330-row scale but probe says don't bother without a reason to override. |
| Blend-weight optimizers that maximize plain OOF accuracy will re-weight toward shift-prone features even when a shift-robust feature family exists | HIGH | Cross-review of gemini sub_021 (`.ai/reviews/claude-reviews-gemini-sub_021.md`): COBYLA blend gives 61% combined weight to shift-prone baseline-family models vs 39% to shift-robust grid-only models, despite exp_003's AUC~0.97-0.998 shift finding; CV 0.9695 but LB only 0.8908 (gap 0.079). Meanwhile claude's sub_020 (grid-only, zero baseline-family weight) scores LB 0.91954, *higher* than gemini's 9-model blend. **exp_011 (done, this block)**: direct blend-weight grid search on claude's own OOF (grid vs RMS-normalized-generic proba), scored by both plain and shift-aware accuracy -- both agree exactly that w=1.0 (pure grid) is optimal; blending in the shift-prone family never helps at any weight. | Confirms the broader claim (don't blend shift-robust with shift-prone) but falsifies the specific "shift-aware scoring would pick a different weight" mechanism -- here plain scoring already found the right answer. Actionable takeaway is simpler than originally proposed: don't blend grid with generic features at all, no reweighting scheme needed. | already spent (exp_011) | done | If gemini/codex use blending, the message is not "reweight the blend" but "drop the shift-prone family from the blend entirely" -- at least for this dataset's grid-vs-generic feature split. Recorded via scheduler (delta 0.0, methodology result). |
| LGBM capacity tuning (num_leaves, min_child_samples) on grid features is a dead lever in both directions | MEDIUM | exp_010 full CV: shallower (leaves=7,min_child=5,n_est=500) vs baseline (leaves=31,min_child=2,n_est=300) -- delta -0.0043 (probe had predicted +0.079, a full-scale reversal). exp_012 high-fidelity probe (full 2330 rows, single 80/20 holdout): deeper (leaves=63) delta -0.0064, deeper still (leaves=127) delta -0.0043. Baseline leaves=31 is a local optimum in both directions. | none -- stop tuning this axis (C9); redirect effort to feature engineering or within-family ensembling instead | ~1.5h spent across exp_010+exp_012 | done, both killed at negative delta | Process lesson: exp_010's tiny 300-row probe predicted the wrong sign entirely -- fixed for exp_012 by keeping full row/class coverage and cutting fold-count instead (single holdout on all 2330 rows), which matched the full-CV-scale direction at ~30x less cost. See MEMORY_CANDIDATES.md for the reusable version of this lesson. |
| Grid-harmonic features are shift-robust in effect even though the raw adversarial AUC stays high | HIGH | exp_006 full: adversarial train-vs-test AUC on grid features is 0.9724 (still >>0.6), but plain OOF (0.9064) vs most-test-like-quartile OOF (0.9160) gap is only -0.0095 -- essentially no accuracy penalty on test-like rows, unlike the generic-feature family (exp_003, AUC~0.998, large measured CV-LB gaps per coordinator note) | resolves PLAN.md priority 1 for the grid feature family specifically: plain CV is trustworthy *for grid-only models*; the CV-LB gap problem is concentrated in generic-feature/blend models, not a universal property of this dataset's shift | already spent (exp_006) | done | Refines the coordinator's priority-1 framing: it's not "CV is untrustworthy under the shift," it's "CV is untrustworthy for feature families where the shift and the class signal overlap." Grid-harmonic features apparently separate those two things (the adversarial classifier can still tell train from test, but not on dimensions that matter for classification accuracy). |
| Rare-tail-class audio augmentation (noise + time-stretch, NOT pitch-shift) could help the 3-56/class imbalance where class_weight reweighting already failed | MEDIUM | exp_009 (this competition) showed class_weight='balanced' hurts both accuracy and balanced_accuracy at probe scale -- reweighting alone doesn't fix the tail; MDPI 2022 systematic review (arxiv/mdpi, logged via writeup.py) recommends noise/time-stretch/mixup augmentation specifically for imbalanced sound classification | untested -- exp_018 queued via scheduler this block, not yet run | ~1h (probe first) | queued (exp_018) | Hard constraint: pitch-shift augmentation is invalid for this task specifically, since Pitch_ID (the label) IS the pitch -- shifting pitch changes the ground truth. Only label-preserving transforms apply (additive noise, time-stretch, not gain jitter since exp_004 already RMS-normalizes). Must augment strictly inside each CV fold's training split only, never before the split, to avoid a rare clip's augmented copy leaking into its own validation fold. |
| Raw-waveform/spectrogram CNN pitch estimators (CREPE, HarmoF0) are not worth pursuing from scratch on this dataset | LOW (rejected) | Research pass (block 3): both architectures are trained on hours of labeled monophonic pitch-tracking audio for continuous f0 regression; our train set is 2330 clips/82 classes (~28/class avg, min 3). PREDICTION.md pre-registered this exact overfit risk before any research reading. | n/a (rejected pre-probe) | n/a | rejected | If reconsidered, the viable version is a frozen pretrained CREPE checkpoint used as a feature extractor (transfer learning), not training a CNN from scratch -- bigger lift than exp_018, not pursued this block. |
| LGBM+MLP blend on grid features (exp_017, proper 3-fold CV, OOF 0.9086->0.9476) submitted as sub_022 -- CV improvement did NOT transfer to LB, and exp_020 shows the standard shift diagnostic can't explain why | HIGH (warning) | sub_022 LB 0.87931 vs sub_020 (LGBM-only) LB 0.91954 -- CV went up +0.039 but LB went DOWN -0.040. `verifiers.py cv-lb --agent claude`: pearson=0.981 but sign agreement only 50% over 3 submissions. Unlike every prior reversal this competition, this was NOT a probe-vs-full-fidelity artifact -- exp_017 used a proper 3-fold CV (fold std 0.0088), confirming exp_016's single-holdout finding. **exp_020 (done, this block)**: applied exp_006's adversarial-quartile-gap diagnostic separately to LGBM-only, MLP-only, and the blend -- gap_lgb=-0.0087, gap_mlp=-0.0129, gap_blend=-0.0078 (blend has the SMALLEST gap of the three). The "MLP is more shift-fragile" hypothesis is falsified; the diagnostic gives all three a clean bill of health and does not predict the LB drop at all. | Score-gate (C1) correctly still treats sub_022 as the local-CV-best (local_score drives the gate, not kaggle_score) -- but flag for the coordinator that sub_020 remains the LB-best, and per the final-selection-by-public-lb memory card, final submission selection should NOT be driven by LB rank alone either. **New actionable takeaway**: a clean adversarial-quartile-gap check is NOT sufficient evidence a CV gain will transfer to LB -- it only reweights TRAIN rows by test-likeness and can't see whatever is actually different about the held-out test set. Treat any further ensemble/blend CV gains on this dataset with a wider safety margin regardless of how clean this diagnostic looks; the actual sub_022 mechanism remains unexplained. | exp_020 done | done, mechanism still unexplained | Do NOT read exp_020's clean-gap result as "the blend is safe after all" -- it's the opposite: the diagnostic that was supposed to catch this kind of problem missed it, so trust it less across the board, including for gemini's/codex's blends. |
| **New lead on the sub_022/024 mechanism**: exp_031 shows the LGBM+MLP blend's predictions track MLP-alone almost 1:1 (w_lgb=0.4 actually means 60% MLP weight) -- blend-vs-MLP disagreement only 9.4%, and on rows where LGBM-alone disagrees with the blend, MLP-alone sides with the blend 87% of the time. | HIGH (pending confirmation) | exp_031 (descriptive, no true labels): see MEMORY_CANDIDATES.md row for full numbers. rare_rate/prior_l1 for MLP-alone almost exactly matches the blend, both far from LGBM-alone. | If true, sub_022/024's LB underperformance is really "MLP's own error pattern on this dataset's shift," not a blend-specific interaction -- meaning any future blend that leans on MLP at all should be treated with the same suspicion as MLP-alone, not evaluated as a fresh architecture. Prepped exp_030 (MLP-alone submission) and exp_032 (w_lgb=0.8, LGBM-dominated blend) this block as two hedge/test candidates for tomorrow, once claude's daily submission cap resets. | exp_030+exp_032 built this block, not yet LB-tested (cap hit today) | prepped, pending tomorrow's submission | Suggest gemini/codex check their own blend weight fractions the same way if they see a similar CV-up/LB-down pattern -- a "moderate" scalar weight can still mean one family dominates the actual predictions. |
| LGBM's OOF errors on this dataset are octave/harmonic-ratio-structured, not random -- 58.0% of 212 OOF misclassifications land at ~7/12/19/24-semitone distances vs 27.3% for random class pairs (cross-referenced against gemini's published YIN median-MIDI mapping) | MEDIUM (new lever, unconfirmed) | exp_034 (own, cheap, no retraining): see MEMORY_CANDIDATES.md row for full numbers. First experiment this competition to examine per-class confusion structure rather than aggregate accuracy/shift metrics. | Motivates exp_035 (queued): a single targeted per-candidate octave-ratio feature (own low-harmonic evidence vs octave-neighbors' low-harmonic evidence), not a generic feature-family variant. Genuinely new angle distinct from the 3 prior "add more variants" attempts (exp_007/014/015, all negative at probe -- dilution). If exp_035 also reverses at probe, that would suggest the octave ambiguity may be physically irreducible from spectral content alone here, closing this lever too. | exp_034 done (0.05h); exp_035 queued (~0.3h probe) | exp_034 done, exp_035 queued for next block | Worth sharing with gemini/codex: checking whether a classifier's errors cluster at harmonic-ratio distances (not just measuring aggregate accuracy) is a cheap, high-information diagnostic for any pitch/f0-classification task. |
| **First candidate to clear the coordinator's shift-aware submission gate (PLAN.md note #2) since the freeze**: exp_033 applies rare-class augmentation (exp_018) to LGBM-ALONE (the sub_020 model family, the only submission confirmed to transfer to LB) instead of to the LGBM+MLP blend family (which has burned two submission slots, sub_022/024). | HIGH | exp_033 full 3-fold CV: plain +0.0094, weighted +0.0127, topq (most-test-like-quartile, the coordinator's own stated gating metric) +0.0189 -- all three positive, unlike any blend-family variant tried. Full-train fit done, `submission_exp033_lgbm_aug_only.csv` banked. | If LB-confirmed tomorrow, this both (a) becomes claude's new best submission and (b) validates a general strategy for this shift regime: isolate which model family actually transfers (via ablations like exp_031), then apply new fixes to THAT family rather than continuing to season the fragile one. | exp_033 CV done (~55min), full-train fit done (~this block) | banked for tomorrow's first submission slot (today's 2/2 cap already spent) | Suggest gemini/codex check whether their own best-transferring submission is a single model or a blend, and route new fixes (augmentation, features) to that specific model family first. |
| A blanket octave-direction override rule (predict the higher/lower of a close-margin octave-pair unconditionally) is NOT a viable fix for exp_034's confusion pattern | MEDIUM (closes a lever) | exp_036 (own, cheap, fold-honest split): "close margin" by median threshold captured 37.5% of all rows, where top1 was already right 88-90% of the time -- blanket override tanked held-out accuracy by -0.088 overall. KILLED at probe. | Confirms the fix needs to be feature-level (giving the model new per-row evidence, exp_035's approach) rather than decision-level (post-hoc override) -- the model isn't uniformly wrong on "confusable" rows, so a uniform rule can't help. | ~0.05h | killed at probe | Share as a methodology caution: a confirmed structured-confusion pattern doesn't mean the model is usually wrong on those rows -- check base accuracy on the targeted subset before designing an override. |
| exp_035's targeted octave-ratio feature reversed sign at full scale (probe +0.0172, full topq -0.0051) -- the grid-harmonic-variant family is now closed for good, 4/4 negative | HIGH (closes a lever, full-scale-confirmed) | exp_035 full 3-fold CV: plain -0.0026, weighted +0.0115, topq -0.0051 vs grid-only, scored the same shift-aware way as exp_033. This is the first FULL-scale confirmation for this family (exp_007/014/015 were probe-only) -- even a mechanism-targeted feature (not a generic elaboration) failed once carried to full CV. | Do not attempt further grid-harmonic feature-family variants on this dataset regardless of how well-motivated (a confirmed error mode, like exp_034's octave confusion, is not sufficient reason to expect a new column to help). Pivoted to exp_037 instead: tuning exp_018/033's augmentation hyperparameters (never itself tuned) under the same shift-aware metric, since exp_033 is the only lever currently clearing the gate. | ~1h (probe+full) | recorded, killed | Promoted the MEMORY_CANDIDATES.md row to HIGH confidence with this full-scale data point. Worth sharing with gemini/codex: even mechanism-targeted feature additions to an already-strong hand-engineered family can dilute rather than help. |
| **New best gate-clearing candidate: XGBoost-alone beats LGBM-alone on the same grid features, no augmentation needed** -- exp_040 full 3-fold CV, all three shift-aware metrics positive (plain +0.0318, weighted +0.0165, topq +0.0257), the best topq delta of the competition, beating both exp_033 (+0.0189) and exp_037/038's augmented LGBM (+0.0206). | HIGH | exp_040 probe (single 80/20 holdout) first showed a cross-metric disagreement (topq +0.0278 but weighted -0.1005) that the full 3-fold CV resolved cleanly in XGBoost's favor on every fold (lgb_acc 0.9189/0.9176/0.8905 vs xgb_acc 0.9459/0.9447/0.9317) -- confirms exp_012/019's lesson that single-holdout probes can flip sign on cross-metric comparisons specifically, not just on the primary metric. | Supersedes exp_033/exp_037-038 as the top-priority banked submission once a full-train fit+predict lands. XGBoost has never been augmentation-tuned (exp_037/038's more_oversample config was only tuned for LGBM) -- exp_043/scheduler-id-exp_042 (probe running this block) tests whether the two independently-validated wins (model family + augmentation) compose additively. | exp_040 probe+full done (~1h); exp_043 probe in progress | full CV done, full-train fit queued this block, exp_043 combo probe running | Different base-learner implementations (leaf-wise vs level-wise growth, different L1/L2 defaults) on the SAME feature set can differ meaningfully in shift-robustness -- worth gemini/codex trying XGBoost-alone on their own best feature sets too, not just LGBM. |
| exp_044 (GBDT-only soft-vote blend, LGBM+aug vs XGBoost+aug) killed at probe -- no blend weight beats the best single model | MEDIUM (closes a lever) | Single 80/20 holdout: XGBoost+aug-alone topq=0.9573 (best single model) vs every blend weight tested (w_lgb 0.3/0.5/0.7) all topq=0.9487, delta -0.0085 vs best single, all three weights identical (blend is dominated by whichever model is stronger, not additive). | Even two orthogonal-inductive-bias GBDTs (leaf-wise vs level-wise), both independently shift-robust, do not combine additively -- extends the "ensembles don't help under this shift" finding beyond the LGBM+MLP family specifically. exp_042 (XGBoost+more_oversample-alone) remains the single best banked candidate; do not spend further probe budget on GBDT ensembling without new evidence. | ~0.3h | killed at probe | Worth sharing with gemini/codex: under this competition's shift, "blend the two best single models" has now failed twice (tree+NN and tree+tree) -- a genuinely orthogonal signal source (not just a different GBDT implementation) would be needed before trying a third ensemble. |
| Feature-alignment/elimination family (CORAL exp_047/048, diagonal alignment exp_048/049, adversarial-importance elimination exp_049/050) is now closed 3/3 negative at full-CV-or-equivalent scale; fold-honest pseudo-labeling (exp_051, adding high-confidence real test rows as training data) is the one surviving open axis with a genuine, if small, positive full-CV result | HIGH (closes 3 levers, opens 1) | exp_047/048 CORAL: topq -0.0256 (probe). exp_048/049 diagonal: topq exactly 0.0 -- discovered to be a mathematical guarantee (tree split-finding is invariant to per-feature monotonic transforms), not empirical evidence, so this sub-family's null result carries less weight than CORAL's genuine negative. exp_049/050 adversarial-importance elimination: probe +0.0085 reversed to full CV -0.0137 (same probe-fidelity-artifact pattern as exp_010/016); a finer drop-fraction sweep (exp_050/051) found the same reweighted-metric-inflates-while-raw-accuracy-craters pattern as exp_046's catastrophic failure and was manually killed without a 2nd full-CV spend. exp_051 (fold-honest pseudo-labeling, real unlabeled test rows added as training data above a 0.85 confidence threshold) full 3-fold CV: plain +0.0021, weighted +0.0011, **topq +0.0017** vs exp_042/sub_025 reference -- small but the first full-CV-confirmed positive result since exp_042 itself. | Confirms C9's pivot-to-different-mechanism guidance was the right call this block (promoting pseudo-labeling once the alignment/elimination family closed, rather than continuing to tune it). exp_051's win is too small on its own to justify a submission today (PLAN.md's "wide margin" bar), but is the first genuinely new lever found since exp_042/sub_025 -- worth compounding with exp_042 rather than treating as a standalone candidate. | ~3h across exp_047-052 | done (3 killed, 1 marginal win recorded) | Share with gemini/codex: (1) the diagonal-alignment null result is a design artifact specific to tree models (monotonic-transform invariance), not evidence that marginal-stat calibration never helps -- don't generalize it to NN-based blends; (2) fold-honest self-training (train stage-1 on train-only, pseudo-label the REAL unlabeled test set, retrain, score only on real held-out labels) is leakage-safe and worth trying on any competition with unlabeled test access, even if the gain here is modest. |
| Density-regularized pseudo-label acceptance (confidence + feature-likelihood combined score) queued as exp_055, extending exp_051's marginal pseudo-labeling win | MEDIUM (queued, unconfirmed) | Research pass block 28: arxiv 2302.14013 (Revisiting Self-Training with Regularized Pseudo-Labeling for Tabular Data) reports rank ~1.8 vs ~3.3 (multiclass) for regularized vs vanilla fixed-threshold self-training, largest gains under limited labeled data -- matches our 2330-row/82-class/3-56-per-class regime. See RESEARCH.md block 28 for full writeup. | If it clears exp_051's own threshold=0.85 result at probe, promote to full 3-fold CV before trusting (same discipline as every other lever this competition). Predicted ceiling is low (exp_051's whole mechanism only moved topq by +0.0017), so expect a small delta even if positive. | not yet run | queued (exp_055, scheduler priority 0.025) | Also logged a counter-evidence flag (RESEARCH.md block 28, Finding 2): arxiv 2307.00079 found class-rebalancing can improve a public eval split while hurting a held-out one collected the same way -- structurally similar to this competition's own CV-vs-LB divergence. Doesn't change any current decision (our `more_oversample` augmentation was already validated via the shift-aware topq metric, not raw CV) but flag for whoever reviews sub_025's eventual private-LB score. |
| **Hierarchical chroma+octave decomposition (exp_058, scheduler id exp_057) -- LANDED NEGATIVE, closes the octave-confusion lever for good (3/3)** -- 2-head classifier (12-way chroma = Pitch_ID%12, then octave = Pitch_ID//12) restructures the training objective itself, unlike exp_035 (feature-add, full CV topq -0.0051) and exp_036 (post-hoc rule, probe -0.088) which both left the flat 82-way objective unchanged. | MEDIUM (closed) | Research pass block 33: arxiv 1804.07297 (Deep Layered Learning in MIR) and arxiv 1906.07145 (Key-Class Invariant Pitch Chroma CNN) motivated chroma/octave factorization; exp_034 measured 58.0% of this model's own OOF errors land at exactly the semitone distances this decomposition targets. Result: full-data probe topq flat -0.0000 despite plain regressing -0.0365 and weighted inflating +0.1285 -- no real improvement. | Closes the octave-confusion lever (3/3 negative: exp_035 feature-add, exp_036 decision-rule, exp_058 hierarchical-decomposition). Do not revisit without a materially new mechanism. See MEMORY_CANDIDATES.md row (3-mechanism-types-failed card). | exp_058 done (~0.5h probe) | closed, negative | Prototypical-network/metric-learning few-shot approach (Finding 5, RESEARCH.md block 33) remains rejected pre-probe -- higher implementation cost, closed-set/few-shot literature mismatch, and this dataset's own NN-encoder experiments are already the most shift-fragile family tried. |
| **exp_056/exp_057 (scheduler id exp_056): pseudo-labeling confidence threshold=0.75 CONFIRMED at full 3-fold CV** -- topq 0.9520->0.9588 (+0.0069), beats thresh=0.85's full-CV result (+0.0017) by 4x. Follow-up sweep (exp_059/scheduler-exp_058, thresholds 0.60/0.65/0.70) found NO further improvement (all <= +0.0085, still below 0.75's own probe number +0.0171) and non-monotonic behavior -- confirms 0.75 is at or near the peak, closing the threshold-sweep sub-axis. Full CV for thresh=0.60 launched block 35 to confirm the probe-scale non-monotonicity at full fidelity. | HIGH | exp_056 full CV (this block): plain +0.0009, weighted +0.0187, topq +0.0069. exp_059 probe: 0.70/0.65 flat (~0.0000), 0.60 +0.0085 -- all worse than 0.75. | Composes with exp_042/sub_025 (XGBoost+aug, topq +0.0343 vs LGBM-no-aug baseline) as an ADDITIVE lever on top -- total predicted topq gain vs original baseline ~+0.041 if it composes as cleanly as exp_040+exp_042 did. Not yet a submission candidate alone (delta +0.0069 is not the "wide margin" the freeze note requires) but worth banking as the next submission once combined with something else or once margin grows. | exp_056 full CV done, exp_059 probe done, exp_058(thresh=0.60) full CV running block 35 | full-CV-confirmed positive, awaiting thresh=0.60 full-CV sanity check | Share with gemini/codex: a coarse-grid threshold sweep whose winner sits at the tested range's edge (not interior) should get a follow-up sweep before treating it as final -- this is the second time this exact pattern showed real signal (see MEMORY_CANDIDATES.md row 27). |
| **EM label-prior-shift correction (Saerens et al. 2002, scheduler id exp_059) LANDED NEGATIVE -- closes domain-adaptation family 3/3.** The EM-estimated test prior does diverge materially from train's (KL 0.30, L1 0.46, one class 41x ratio), so the mechanism had real room to matter -- but applying the correction to validation posteriors made accuracy much worse: plain 0.9442->0.8133, **topq 0.9573->0.7949, delta -0.1624**. | MEDIUM (closed) | RESEARCH.md block 35/36 Finding 6: `scheduler.py record --id exp_059 --stage probe --delta -0.1624`, killed at probe per C4/C9 (no full-CV spend justified). | Domain-adaptation/distribution-correction family now closed 3/3 (CORAL, diagonal, EM prior) with three DISTINCT failure mechanisms (off-diagonal alignment moved metrics but hurt; diagonal was a mathematical no-op for trees; EM prior estimate too noisy at ~7 test-rows/class average and actively harmful when trusted). Do not revisit without a mechanism that specifically handles small-sample prior estimation. | exp_059 done (probe only) | closed, negative | Share with gemini/codex: EM/Saerens-style prior-shift correction needs enough test rows per class for its own prior estimate to be trustworthy -- with ~7/class average and many classes at 0-3, the "cure" outweighs a real, measured disease. |
| **exp_058 (scheduler id exp_058) full CV CONFIRMED: pseudo-labeling threshold=0.60 beats threshold=0.75, new best full-CV-confirmed lever since sub_025.** Resolves block 35's open question and reverses the probe-scale non-monotonicity (exp_059's probe had 0.75 probe +0.0171 > 0.60 probe +0.0085) -- full 3-fold CV: plain +0.0009, weighted +0.0180, **topq +0.0137**, ~2x exp_056's thresh=0.75 full-CV result (+0.0069). | HIGH | `scheduler.py record --id exp_058 --stage full --delta 0.0137` (`full_cv_exp058.log`). Another instance of a probe-scale rank flipping at full CV (see exp_010/016/035 for the pattern this competition keeps hitting). | Clears the coordinator's shift-aware submission gate (PLAN.md note #2) against the sub_025-equivalent baseline (exp_042's own topq=0.9520 reference in this same log) by the widest margin of any pseudo-labeling variant tried. Full-train fit (`fit_full_exp058_pseudolabel_thresh060.py`) launched this block -- top submission priority. | full CV done, full-train fit running | full-CV-confirmed positive, submission in progress | Same lesson as exp_040/exp_012: when a probe and full CV disagree on ranking (not just magnitude), trust the full CV -- this is now the 4th+ time this exact pattern has appeared in this competition. |
| **exp_061 (scheduler id exp_060) queued: cross-model (XGBoost+LGBM) agreement-gated pseudo-labeling** -- research pass block 37, motivated by arxiv 2510.07509 (multimodal co-training under distribution shift). Distinct mechanism from single-model confidence threshold (exp_051-059, closed at thresh=0.60 peak) and from blend-of-final-predictions (exp_017/044, closed 2/2 negative) -- agreement here only gates which real test rows get pseudo-labeled; final model stays XGBoost-alone. | MEDIUM (probe running) | RESEARCH.md block 37 Finding 7. Probe sweeps agree_threshold 0.50/0.60/0.70 vs exp_058/059's single-model thresh=0.60 topq reference (0.9658, probe scale). | If positive, composes with (doesn't replace) exp_058's confirmed pseudo-labeling win as a higher-precision label-selection rule; if negative, the pseudo-labeling family's remaining live sub-axis (confidence threshold) is fully explored and the next search needs a mechanism outside self-training entirely. | probe launched block 37, not yet landed | probe running | n/a yet |
| **exp_062 (scheduler id exp_061) LANDED NEGATIVE: shrinkage-regularized (Dirichlet/James-Stein) label-shift prior correction does NOT recover a positive delta at any regularization strength.** Alpha sweep {1,5,20,50} on topq: -0.0855/-0.0513/-0.0085/-0.0085 -- monotonically improving toward 0 as shrinkage strengthens (more weight on the trusted train prior, less on the noisy EM estimate) but never crossing back to non-negative even at alpha=50 (near-total shrinkage). Alpha=0 sanity check exactly reproduces exp_059/060's raw-EM -0.1624, confirming the harness is correct. | HIGH (closes a lever definitively) | `scheduler.py record --id exp_061 --stage probe --delta -0.0085`. This is a STRONGER closure than exp_059/060 alone: it rules out "the raw EM estimator was too aggressive" as the explanation (regularizing it away doesn't help either), not just "the specific estimate was wrong." | Label-shift prior-correction family now closed 2/2 negative (raw EM exp_059/060, shrinkage-regularized exp_061/062). Do not revisit with a 3rd regularization variant -- the mechanism itself (reweighting posteriors by an estimated test-side class prior) appears to lose more than it gains on this dataset's ~7-test-rows/class-average regime, regardless of estimator noise-control. | probe done (~0.2h) | closed, negative | Share with gemini/codex: if trying label-shift/prior correction on a similarly thin-per-class-count test set, regularizing the prior estimate (Dirichlet/James-Stein shrinkage) is not a fix by itself -- worth checking whether the correction helps at all before tuning its regularization strength. |
| **exp_063 (scheduler id exp_062) LANDED NEGATIVE: graph-based label propagation (sklearn LabelSpreading) does not beat single-model confidence pseudo-labeling.** Check 1 (graph-only accuracy on held-out X_va): far below stage-1 XGBoost's own fit at every config tried (plain 0.76-0.82 vs XGBoost's 0.94) -- the graph alone is a much weaker classifier on this feature space, though rare_acc stayed flat (0.8125) so the weakness isn't concentrated on thin classes specifically. Check 2 (graph-propagated pseudo-labels, XGBoost retrains): thresh=0.5 (431/583 pseudo-labeled) topq -0.0086; thresh=0.7 (289/583 pseudo-labeled) topq -0.0000 (~flat). | HIGH (closes the pseudo-labeling mechanism-family search) | `scheduler.py record --id exp_062 --stage probe --delta -0.0086`. **Bookkeeping note**: this row's scheduler slot was momentarily mis-recorded mid-block-40 with the shrinkage-EM result (a copy-paste mix-up reading the wrong probe log against the wrong id) before being corrected back to its own content directly in EXPERIMENTS.jsonl -- the shrinkage-EM result was already correctly recorded under exp_061 by a prior session, so no data was lost, but flag this if the scheduler-id vs descriptive-exp-number offset convention keeps causing this class of mistake. | Pseudo-labeling mechanism-family search (confidence threshold, cross-model agreement, EM/shrinkage prior correction, graph propagation) is now exhausted across every inductive-bias variant tried -- only the confidence-threshold axis (exp_058, thresh=0.60) ever cleared zero. Do not add a 5th pseudo-labeling mechanism variant without a materially different reason to expect a different outcome (C9). | probe done (~0.3h) | closed, negative | Share with gemini/codex: on this dataset's rare-class-thin (14/82 classes <10 samples) regime, semi-supervised graph methods lose to a supervised classifier's own confidence by a wide margin -- kNN-graph anchors are too sparse for thin classes to make propagation competitive. |
| **exp_064 (scheduler id exp_063) queued: per-clip (row-wise) z-normalization (CMVN-style) of the grid-harmonic feature vector** -- research pass block 40, motivated by CMVN (Wikipedia; IBM Research utterance-specific normalization) and exp_003/004's per-recording loudness/noise-floor shift diagnosis. Genuinely distinct from the closed CORAL/diagonal-alignment axis: exp_049 proved column-wise affine rescaling is mathematically neutral for tree models (monotonic per-column transform can't move histogram splits); row-wise normalization mixes information across columns per-row, so it isn't covered by that invariance and can actually change what XGBoost sees. | MEDIUM (probe launched, uncertain sign) | RESEARCH.md block 40 Finding 9. Single 80/20 holdout probe (`probe_exp064_rownorm.py`), same split/seed/augmentation/model as exp_048-063. | Pre-registered risk: the ~405 columns mix heterogeneous feature families (mfcc/chroma/contrast/zcr/tonnetz) with different natural units -- z-scoring a row across all of them together could destroy real inter-family signal along with whatever per-clip level shift it removes. If negative, this is the last identified fresh mechanism family before the search needs to pivot to combining already-confirmed levers rather than finding a 5th new one. | probe launched block 40 | probe running | n/a yet |
| **exp_066 (LightGBM few-shot hyperparameters, arxiv 2411.04324) LANDED CATASTROPHIC (-0.6325), KILLED -- but its own ablation control accidentally surfaced this block's most important open lead.** The few-shot config (min_data_in_leaf=1, num_leaves=4, extra_trees=True) collapsed topq exactly like every other "fit train harder" lever this competition (exp_046 importance-weighting -0.6667, exp_060 EM prior-shift -0.1624). Its reference/ablation run in the SAME script -- LightGBM's TRUE stock defaults (min_data_in_leaf=20) + the same more_oversample augmentation used everywhere else -- scored topq=0.9915, a **+0.0342 delta vs the currently-banked XGBoost+aug baseline (0.9573)**, the single largest lever found this competition if it holds. | HIGH (unconfirmed, single 80/20 holdout only) | `exp067_probe.log`: lgbm_default plain=0.9421 weighted=0.9827 topq=0.9915 vs XGBoost+aug reference plain=0.9442/weighted=0.8223/topq=0.9573. **This contradicts exp_044's GBDT-blend probe**, which found LGBM+aug weaker than XGBoost+aug (blend dragged down to topq=0.9487 at every weight) -- but exp_044 used `min_child_samples=2` (a LightGBM alias for the SAME parameter as `min_data_in_leaf`, and a highly permissive value), never the true default of 20. exp_044 and exp_066 were never an apples-to-apples comparison of LGBM vs XGBoost -- they differed on this one regularization knob too. | Queued exp_068/exp_069 (scheduler id exp_068): a controlled 6-point `min_data_in_leaf` sweep (2/5/10/20/30/50) on the identical split/aug/harness, isolating this single variable before trusting either prior result. Also queued exp_069/exp_070 (scheduler id exp_069): the analogous XGBoost `min_child_weight` sweep (1/5/10/20/40, XGBoost's own leaf-size-equivalent knob, never tuned since exp_040), testing whether "more restrictive regularization = more shift-robust" generalizes to the currently-banked model family too. Both launched concurrently this block; if the LGBM leaf-size effect confirms at full CV, it would roughly double the total measured topq gain of the competition (current best composed total ~+0.048 from exp_042+exp_058) and should become the next submission candidate ahead of any further pseudo-labeling variants. | probes launched block 41, not yet landed | probe running (both) | Flag for gemini/codex if they are also running LightGBM: `min_data_in_leaf`/`min_child_samples` defaults (20) may be doing real shift-robustness work by preventing overfit to thin classes -- worth checking before assuming a lower value (more permissive splitting) is always better for a many-class/thin-class problem under train/test shift. |
| **exp_067 (NearestCentroid prototypical classifier) LANDED NEGATIVE, closes the centroid/metric-learning base-model axis.** All variants (raw std, PCA20/50/100) scored well below XGBoost+aug on topq (-0.1881 best of 4, PCA20 worst at -0.5385). | MEDIUM (closed) | `exp068_probe.log`. | Distance-to-centroid underfits this 405-dim feature space badly; PCA compression made it strictly worse at every tested dimensionality (20/50/100), not better, so the failure isn't simply "too many noisy dims" -- the centroid inductive bias itself is a poor fit here relative to GBDT axis-aligned splits. Do not revisit without a fundamentally different distance metric or learned embedding (not just PCA). | probe done (~0.15h) | closed, negative | n/a |
| **sub_026 SUBMITTED AND CONFIRMED: new best across all three agents.** exp_058 (thresh=0.60 fold-honest pseudo-labeling on exp_042's XGBoost+aug, full CV topq +0.0137) had been full-CV-confirmed and full-train-fit in a prior block but never actually submitted -- found the banked CSV still sitting unsubmitted this block, ran the pre-submit checks (`memory_cli.py retrieve --anchor pre-submit`, `verifiers.py cv-lb`, `evaluate.py`) and submitted via `submit.py --force` (same C1-override justification as sub_025). | HIGH (confirmed on LB) | **public 0.94827 / private 0.95110** vs sub_025's 0.94252/0.93643 -- a further +0.0058/+0.0147 gain, consistent in direction and rough magnitude with exp_058's measured full-CV topq delta (+0.0137). `verifiers.py cv-lb --agent claude` sign agreement improved 50%->60% over 6 submissions post-sync. | This is now claude's (and the competition's) best submission. Next lever to test against it: exp_069/exp_070's regularization sweeps (LGBM `min_data_in_leaf`, XGBoost `min_child_weight`), in progress as this row is written -- if either confirms a real full-CV win, stack it on top of exp_058's pseudo-labeling rather than exp_042 alone. | already spent | submitted, confirmed new best | Share with gemini/codex: a fully-validated, full-CV-confirmed, full-train-fit submission candidate had been sitting banked and unsubmitted since block 36 (~2.5h of wall clock across several intervening blocks) -- worth double-checking your own workspace for banked-but-unsubmitted candidates before assuming the next experiment is the way to improve, especially after a `/day` session boundary. |
| **Tooling gap: `submit.py --force` prints a misleading "Score gate PASSED" message even when the score does NOT beat current best.** Line ~105 of `tools/submit.py` gates the real comparison on `if current_best and not args.force`, so when `--force` is used the comparison is skipped entirely and execution falls through to the unconditional `if current_best: print("Score gate PASSED: {local} beats current best {best}")` block -- printing "PASSED...beats" even when `local < best` (observed directly on sub_026: message said "0.9472 beats current best 0.9523" which is false). | LOW (cosmetic, but could mislead a future session that greps logs rather than re-deriving the comparison) | Directly observed in sub_026's submit.py output this block; did not change the actual decision since the override was already deliberate and separately justified, but flag it distinctly from the existing "no shift-aware field in best_scores" gap (PLAN_DRAFT's exp_042/sub_025 row) so the two known gaps aren't conflated. | Suggest `submit.py` either suppress the PASSED message under `--force` or compute and print the real local-vs-best comparison regardless of whether it's gating. | n/a (observation only) | flagged, not fixed (out of scope for a workspace-isolated agent to patch shared tooling without review) | Share with gemini/codex: if you use `--force`, don't trust the printed "PASSED" line as confirmation your score actually beat the registry's `best_scores` -- re-check the numbers yourself. |
| **exp_068 (LGBM `min_data_in_leaf=20` true-default full CV) LANDED NEGATIVE, closes the LGBM-defaults-beat-XGBoost hypothesis for good.** Full 3-fold CV topq=0.9468 vs banked XGBoost+aug's 0.9520, delta -0.0051 -- reverses exp_066's single-holdout incidental-ablation claim of +0.0342, which was noise (see MEMORY_CANDIDATES.md's new "incidental ablation control" card). | MEDIUM (closes a lever) | `full_cv_exp068.log`. | XGBoost remains the confirmed best model family; do not revisit LGBM hyperparameters on this axis again. | already spent | closed | n/a |
| **CONFIRMED: exp_073/074 -- wavelet scattering transform features (kymatio `Scattering1D`, J=8/Q=8, 8kHz, mean+std pooled, 468 feats) ARE the first new feature family since exp_005 to beat grid-harmonic-alone, but only concatenated, not standalone.** Scattering-alone catastrophically fails (topq=0.6838 vs grid-alone 0.9573, matches CREPE/PANNs pattern -- see STRATEGY.md, this competition has now rejected 3/3 alternative single-family feature extractors). grid+scattering concatenated: first probe looked flat (+0.0085) but that comparison was CONFOUNDED (scattering isn't augment-compatible, so the concat run had augmentation silently dropped while the baseline constant it was compared to did NOT); a same-row-filter no-aug-vs-no-aug control (exp_074) deconfounded it to +0.0171, then full 3-fold CV (fold-safe, both arms unaugmented) confirmed it at **topq +0.0223, winning on all 3 individual folds** (0.9498>0.9459, 0.9485>0.9447, 0.9407>0.9317). CAUTION: adversarial train/test AUC jumps from 0.9724 (grid-only) to 0.9975 (grid+scattering) -- scattering coefficients are far more shift-discriminative (likely loudness/noise-floor recording-condition signal per exp003/004, not pitch content), so the topq gating metric may be less trustworthy here (near-perfect adv AUC concentrates weight on very few validation rows). Confirmed via exp_075: pseudo-labeling (thresh=0.60, the single best composable lever on grid-only) composes NEGATIVELY with grid+scattering (-0.0085 probe) -- plausible mechanism is the high adv-AUC model's confident test predictions reinforcing shift artifacts rather than content signal. NOT submitted yet -- augmentation-composition (exp_076, queued, needs extending `augment_rows` to run kymatio on augmented audio) is the next step before this becomes a submission candidate; on its own (no aug, no pl) it only ties sub_026's topq (0.9657 both), not a clear win, and carries the flagged reliability caveat. | HIGH (only open new-mechanism lever left; if exp_076 composes positively this could be the next real submission jump) | `full_cv_exp073_grid_scattering.py`/`full_cv_exp073.log`, `probe_exp074_grid_noaug_control.py`/`exp074_probe.log`, `probe_exp075_scattering_pseudolabel.py`/`exp075_probe.log`. | Confirmed +0.0223 topq standalone; unknown whether it stacks with augmentation (queued exp_076) since it already failed to stack with pseudo-labeling. | ~40min full CV (spent) + ~30min for exp_076's augmentation extension (next block) | confirmed positive, held pending exp_076 | Share with gemini/codex: (1) `kymatio` needed a scipy>=1.15 compat monkeypatch (`sph_harm` -> `sph_harm_y` shim, see `extract_scattering_features.py`) to import on this box's environment; (2) methodology lesson -- when a new feature family can't go through your existing augmentation/perturbation pipeline, comparing it against a baseline constant measured WITH augmentation silently confounds two changes at once (feature addition + augmentation removal); always build a same-row-filter, same-preprocessing control before trusting the delta, not just a hardcoded baseline constant. |
| **sub_027 submitted and CONFIRMED new best (own): pseudo-label threshold 0.60 -> 0.50 (exp_071's full-CV-confirmed lower-threshold-sweep winner), otherwise identical to sub_026's pipeline.** Full 3-fold CV topq=0.9674 vs sub_026-equivalent's 0.9657, delta +0.0017 -- small but measured at full fidelity, not probe. `--force` used (same C1-override justification as sub_025/026). **Kaggle public score 0.95402, beats sub_026's 0.94827 by +0.00575** -- a small full-CV delta (+0.0017 topq) transferred to an even larger LB gain, cv-lb sign agreement improved to 67%/7 post-submission (from 60%/6). | MEDIUM (small confirmed refinement, not a new mechanism) -- but the LB transfer ratio (LB gain > CV gain) is itself worth noting | `full_cv_exp071_lower_threshold_sweep.py`/`full_cv_exp071.log`, `fit_full_exp071_pseudolabel_thresh050.py`. | +0.0017 topq over current best measured; +0.00575 public LB realized. | already spent (full-train fit was already banked from a prior block, this block only ran the submission checks) | submitted, confirmed new best (public 0.95402) | n/a |
| **exp_076 (scattering + rare-class augmentation composition) LANDED FLAT (+0.0000), closing the composition question for good: grid+scattering is now capped at its own standalone +0.0223, since neither of the two previously-confirmed-independent levers (augmentation: flat; pseudo-labeling, exp_075: -0.0085) adds anything on top of it.** `probe_exp076_scattering_augment.py`: grid+scattering no-aug topq=0.9658 vs grid+scattering+aug (399 extra rows) topq=0.9658, delta 0.0000 (plain also flat; weighted actually dropped -0.0449). Recorded via `scheduler.py record --id exp_074 --stage probe --delta 0.0`, KILLED. | MEDIUM (closes a composition question, doesn't open a new one) | `wait_exp076_exp077.log` (`probe_exp076_run.log` section). | Grid+scattering standalone (no aug, no pl) topq=0.9658 remains BELOW the currently-banked sub_027 pipeline (XGBoost+aug+pseudolabel(0.50), full CV topq ~0.9727) -- not a submission candidate on its own unless a new mechanism composes with it positively. | already spent | closed, negative/flat | n/a |
| **exp_078 (own, new): does log1p-compressing wavelet scattering coefficients before mean/std pooling (standard Anden/Mallat scattering practice, never applied here) both add signal and reduce grid+scattering's flagged near-perfect adversarial AUC (0.9975)?** Motivated directly by the exp_073/074/075 risk flag (scattering may be encoding loudness/recording-condition shift, not just pitch content) -- log-compression is the standard fix for heavy-tailed, energy-dominated scattering coefficients in the audio-classification literature and could plausibly reduce that exact shift-encoding rather than just gate around it. Cheap subsample probe (500 train/200 test, single Scattering1D call per file reused for both raw and log pooling, no extra audio decode) written and launched this block (`probe_exp078_log_scattering.py`). | HIGH if it both improves topq AND lowers adv AUC (would reopen the pseudo-labeling composition question too, since exp_075's failure was attributed to the high adv AUC) | pending -- launched this block, result not yet landed. | If positive: full re-extraction (~50min, same cost as original) adding log-pooled columns, then retest pseudo-labeling/augmentation composition from scratch since the adv-AUC-driven failure mode may no longer apply. If flat/negative: scattering well stays capped at +0.0223, genuinely need a new mechanism family. | ~20-30min probe (in progress) | probe running | n/a yet |
| **exp_077 (16kHz-vs-8kHz scattering Nyquist-cutoff probe) crashed on a stratification bug (fixed, relaunched this block), not yet landed.** Original script used a plain (non-stratified) `train_test_split` on a coarse ~500-row/82-class subsample -- with that few rows per class, some classes ended up entirely in the validation split, so XGBoost's `num_class` (fit from the full label set) didn't see them in `y_tr`, crashing with "Invalid classes inferred". Fixed by (1) dropping singleton classes before splitting and (2) adding `stratify=y_all` to the split; also added parquet caching of the extracted 16kHz features so a second crash wouldn't waste the ~22min extraction again. Relaunched (`probe_exp077_scattering_higher_sr.py` -> `exp077_probe_v2.log`). | LOW-MEDIUM (addresses a flagged-but-untested risk, not a confirmed lever) | pending. | If 16kHz recovers meaningful signal specifically on the high-median-freq tertile, would justify a full 16kHz re-extraction (~2x the 8kHz cost, ~100min) before committing scattering fully to 8kHz. | ~15-20min re-run (in progress) | probe running | Process note for future sessions: any probe script that subsamples rows across many classes (this dataset has 82 classes, several with <10 total samples) MUST either stratify the train/val split or filter/handle classes with <2 members before fitting a multiclass XGBoost model with a fixed `num_class` -- a plain `train_test_split` silently risks this crash whenever the subsample is small relative to the class count. |
| **exp_077-v2/exp_078 BOTH LANDED this block. exp_078 (log1p-compress scattering) is flat (0.0000), closing the log-compression axis cleanly -- no gain, and since it's identical raw-vs-log, it can't be lowering adv AUC either.** exp_077-v2 (16kHz scattering) shows a **suspicious** probe result: topq +0.0588 overall (0.7941 vs 0.7353) but `topq_high_freq_tertile` -- the exact subgroup the Nyquist-truncation mechanism predicts should move -- is IDENTICAL (0.7317) between 8kHz and 16kHz. The aggregate lift moving while the targeted subgroup doesn't move at all is a red flag for probe noise (n=34 topq subset) rather than a real SR effect. Scheduler mechanically promoted exp_075 to full CV anyway (delta>0 rule has no mechanism-check exception) -- rather than skip it on a judgment call, launched the full 16kHz extraction+3-way-CV (grid-only/grid+scattering-8k/grid+scattering-16k) in the background (`exp075_extract_scattering_16k_full.py` -> `exp075_full_cv_scattering_16k.py`, chained, ETA ~1.5-2.5hrs) since it's the only way to resolve noise-vs-real cheaply enough to trust, and it doesn't block other work (runs on the box's only occupied core). | LOW (my prior, given the null high-freq-tertile result, is that this closes flat/negative) but MEDIUM-HIGH if it somehow confirms (would be the first scattering-composable lever to add anything since standalone) | `exp077_probe_v2.log`, `exp078_probe.log`, `wait_exp077v2_exp078.log`. `exp075_extract_full.log`/`exp075_full_cv.log` pending. | If full CV confirms the lift AND the high-freq-tertile subset also moves at full scale (n will be much larger than the probe's n=41), 16kHz becomes the new scattering default (extra ~50min one-time re-extraction cost). If flat/negative (expected), the scattering axis is fully exhausted (6/6 non-positive-beyond-standalone: SR, log-compression, augmentation, pseudo-labeling all fail to add). | ~1.5-2.5hr full extraction+CV (in progress, background) | full CV running | Share with gemini/codex: when a scheduler/gate mechanically promotes a probe result based on a single delta>0 threshold, check whether a domain-specific targeted-subgroup metric was collected alongside the aggregate one (here, `topq_high_freq_tertile`) -- if the subgroup the hypothesis predicts should move is flat while the aggregate moves, that's evidence of noise even when the mechanical rule says "promote." |
| **Research pass (this block) found no new mechanism family, confirming rather than contradicting STRATEGY.md's dry-well assessment.** Searched missing-fundamental/virtual-pitch psychoacoustic algorithms (Terhardt/Goldstein) and 2025 supervised-contrastive/prototypical few-shot audio papers. Both rejected: virtual-pitch template matching is the same per-candidate-f0 harmonic-scoring family as the existing grid features (a reweighting variant, like the already-rejected GFCC); contrastive/prototypical embeddings are the same centroid-classification decision rule already killed at probe (exp_067/068, topq -0.1881) plus a from-scratch deep encoder on top, and frozen-pretrained-embedding domain mismatch already sank CREPE/PANNs (exp_028/029). | LOW (explicit rejections, not a new lever) | RESEARCH.md 2026-07-08 (/day) entry. | None queued. | ~20min research pass | rejected, documented | If exp_075's full CV also closes negative, recommend the coordinator/human weigh in on whether to keep searching for new mechanisms vs. shift fully to (a) the grid+scattering hedge-submission decision for shake-up-risk diversification and (b) polishing the banked pipeline's existing composition. |
| **sub_028's public score synced this block: 0.94827, confirming the pre-registered prediction that the grid+scattering hedge would score below sub_027 (0.95402) on LB, not just CV.** Also: `verifiers.py cv-lb --agent claude` sign agreement crossed the coordinator's >70% trust threshold for the first time this competition (67%/7 -> 71%/8). | MEDIUM (both are confirmations, not new levers) | `sync_scores.py` output this block; `verifiers.py cv-lb --agent claude`. | No pipeline change -- but the cv-lb crossing suggests the shift-aware topq gate has stabilized as a genuinely predictive proxy after ~8 submissions, worth flagging to the coordinator as evidence the freeze-note protocol (block 2026-07-07) is working as intended. | already spent (routine sync+check) | done | Share with gemini/codex: if their own `cv-lb` sign agreement is still <70%, the topq-style shift-aware gating approach (not raw CV) may be the fix, per this competition's own trajectory (50% -> 54% -> 60% -> 67% -> 71% as more submissions passed through the shift-aware gate instead of raw CV). |
| **exp_075 (16kHz-vs-8kHz scattering full 3-fold CV) LANDED NEGATIVE, closes the wavelet-scattering mechanism family 6/6 non-positive-beyond-standalone -- this was the last identified open mechanism family in the competition.** Full CV: grid+scattering-16k topq=0.9605 vs grid+scattering-8k topq=0.9657, delta -0.0051, reversing the probe's suspicious +0.0588 lift exactly as the probe's own `topq_high_freq_tertile` null result predicted (see PLAN_DRAFT row 46). | HIGH (closes the last open lever) | `exp075_full_cv.log`, `scheduler.py record --id exp_075 --stage full --delta -0.0051`. | Scattering stays capped at its confirmed standalone +0.0223 (banked as sub_028's hedge). No further scattering-family probe budget without a materially different transform. | already spent (background, ~1.5h) | closed, negative | Share with gemini/codex: this is now the 5th+ time a small-n probe (n=34-117) flipped sign at full CV in this competition -- if either agent is trusting single-holdout probes on <150-row validation splits for a go/no-go call, raise the probe fidelity (more folds or more rows) before trusting the direction, not just the magnitude. |
| **Tooling/data-integrity gap: scheduler-id `exp_060`'s recorded `probe_delta` (-0.1624) does not match its own probe log.** The record's `idea` field correctly describes cross-model (XGBoost+LGBM) agreement-gated pseudo-labeling, and its `notes` field correctly cites `probe_exp061_agreement_pseudolabel.py` -- but that script's actual log (`exp061_probe.log`) shows delta-vs-single-model-baseline of -0.0085 (thresh=0.5) or +0.0000 (thresh=0.6/0.7), never -0.1624 (which is EM prior-shift's number, exp_059's result). Likely a stale copy-paste from an earlier record, distinct from the exp_062 mix-up note 34 already flagged as fixed. Does not change any decision (the disposition "killed" is correct either way -- cross-model agreement never beat single-model confidence), but the stored magnitude is wrong and could mislead a future session skimming deltas rather than logs. | LOW (cosmetic, decision-preserving) | Directly compared `EXPERIMENTS.jsonl`'s exp_060 record against `exp061_probe.log` this block. | Suggest a coordinator/human spot-check EXPERIMENTS.jsonl deltas against their source logs periodically -- this is the second such copy-paste mismatch found this competition (see note 34), both in the pseudo-labeling family's dense run of same-block probes. | ~5min (observation only) | flagged, not corrected in place (out of scope to rewrite ledger history without review) | n/a |
| **exp_080 CONFIRMED POSITIVE: ExtraTreesClassifier (extremely randomized trees) is a genuinely new model family that beats XGBoost-alone on grid features.** Full 3-fold CV: plain +0.0026, weighted +0.0215, topq +0.0017 (coordinator's gating metric) -- small but consistent in sign across all three metrics, confirming the probe direction. Motivated by 2025 few-shot-tabular literature (ExtraTrees' extra split-point randomization as implicit regularization on thin per-class counts, 82 classes/~28 rows-class avg). | MEDIUM (small confirmed lever, first new base-model-family win since XGBoost>LGBM itself) | `full_cv_exp080_extratrees.log`, `scheduler.py record --id exp_080 --stage full --delta 0.0017`. | Not yet a standalone submission candidate (topq 0.9451 vs the banked pipeline's ~0.9674) -- candidate for ensemble diversity (queued exp_081) or as the base model under scattering (queued exp_082) instead. | already spent (~340s full CV) | closed, positive | n/a |
| **exp_082 LANDED NEGATIVE: the confirmed scattering feature win (+0.0223 topq for XGBoost, exp_073/074/079) does NOT transfer to ExtraTrees -- it's XGBoost-specific, not a property of the feature family alone.** Full 3-fold CV: ExtraTrees on grid-only topq=0.9451 vs ExtraTrees on grid+scattering topq=0.9348, delta **-0.0103** (plain -0.0172, weighted a much larger -0.2018) -- scattering actively hurts ExtraTrees, the opposite direction from XGBoost. Likely mechanism: ExtraTrees' extra-random split selection can't exploit the same fine-grained structure in the 468 additional scattering coefficients that XGBoost's gradient-guided splits do, so the extra columns just add noise to an already-more-random split search. | MEDIUM (closes a feature-family x model-family interaction question, reinforces that feature wins are not automatically model-agnostic) | `full_cv_exp082.log`, `scheduler.py record --id exp_082 --stage full --delta -0.0103`. | Do not assume a confirmed feature-family win generalizes across model families without testing -- this is the first direct counter-example this competition (scattering's standalone win was previously only tested on XGBoost). | already spent (~336s full CV) | closed, negative | Share with gemini/codex: if either is using scattering-style engineered feature additions validated on one model family, check they still help before assuming the gain is feature-inherent rather than model-specific. |
| **exp_079 (Q4-vs-Q8 wavelet-scattering-parameter sweep, full CV confirmation) LANDED NEGATIVE, closing the scattering Q-parameter axis and reversing the probe's own signal -- the 7th+ probe-vs-full sign/magnitude reversal this competition.** Probe (500/200 subsample, n=34) found Q4 topq=0.7647 beating the banked Q8 reference topq=0.7353 by +0.0294, with a clean monotonic 3-point gradient (Q16 worse still) suggesting a real, non-noise effect. Full 3-fold CV on the complete dataset reversed this: grid+scattering-Q8 topq=0.9657 vs grid+scattering-Q4 topq=0.9623, delta **-0.0034** (Q8 vs Q4) -- Q8 remains the better setting. Both Q4 and Q8 still beat grid-only (topq 0.9434) by a wide margin, so the scattering feature family itself stays confirmed; only the specific Q=4 tuning attempt fails. | MEDIUM (closes a sub-axis, doesn't change the standalone scattering win) | `full_cv_exp079_q4.log`, `scheduler.py record --id exp_079 --stage full --delta -0.0034`. | Keep the banked Q=8 scattering config; do not revisit the Q-parameter axis again without a materially different sweep design. | already spent (~4230s full CV, extraction-dominated) | closed, negative | Reinforces the standing methodology card (MEMORY_CANDIDATES.md #32): a clean-looking monotonic probe gradient on a tiny subsample (n=34) is still not sufficient evidence on this dataset -- full-CV confirmation is required before trusting even a "clean" multi-point probe pattern, not just a single-point flip. |

| exp_083 (nested 3-way blend) | claude | full CV in progress as of block ~125 (this /day session, pid 279975, fold 0/3 done at block start) | n/a | Own prior-session launch; corrected-methodology run per blend-topq-lb-mismatch.md/exp_084 (nested leave-one-fold-out weight selection, magnitude-floor check built in). Prepped exp_085 (stacking meta-learner, LogisticRegression on exp_083's OOF arrays, own follow-up) to launch the instant exp_083's .npy files land -- tests the "trained meta-learner, not yet tried" mechanism STRATEGY.md item 0b flagged, sidesteps scalar-weight leakage by construction (meta-learner fit via its own nested CV). |
| exp_085 (trained stacking meta-learner) | claude | LogisticRegression stacking meta-learner over exp_083's 3 saved OOF arrays, nested leave-one-outer-fold-out (zero retraining) -- tests the one remaining "materially different weight-selection mechanism" flagged by the blend-topq-lb-mismatch card. | nested topq(meta)=0.9520 vs nested topq(A+B ref)=0.9708, delta -0.0189 (WORSE than the scalar weight-sweep's nested -0.0051) | already spent (~75s, cheap: array loads + small LR fits, no XGBoost/ExtraTrees retraining) | closed, negative | A trained meta-learner has far more parameters than a scalar search and overfits its own nested fit harder at this data scale (~19 rows/class in the meta-training set) -- more combiner flexibility makes blending worse here, not better. Closes prediction-level blending/stacking 6/6 non-positive on this competition by every weight-selection method tried; do not revisit without a fundamentally different data regime (more rows/class), not just a different combiner. Updated `.ai/memory/failures/blend-topq-lb-mismatch.md` and escalated as a PLAN.md coordinator follow-up. |
