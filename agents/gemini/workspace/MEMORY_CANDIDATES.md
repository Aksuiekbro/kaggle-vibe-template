# Gemini Memory Candidates

Draft memory-card candidates here. Do not write directly to `.ai/memory/` during active competition work.

| Claim | Scope | Evidence | Counter-evidence | Predicted impact | Status |
|-------|-------|----------|------------------|------------------|--------|
| Vectorized index slicing and direct dot-product ACF yields massive speedup over boolean masking and full-mode cross-correlation for audio signal processing. | general signal processing | ~3.6x speedup on 5 files, identical validation results. | none | High speedup in preprocessing time. | candidate |
| LightGBM hyperparameter optimization via Optuna provides a massive accuracy boost (+0.44) over baseline settings on high-dimensional raw spectral features when classes are highly imbalanced and data is sparse. | GBDT audio classification | 5-Fold CV Accuracy improved from 0.2957 to 0.7378. | none | Extremely high accuracy improvement. | candidate |
| Out-of-fold probability ensembling of two GBDTs trained on different audio feature families (baseline vs combined-clean with adversarial selection) yields strong generalization and CV boost (+0.024) even under covariate shift. | GBDT audio classification | CV Accuracy improved from 0.7378 to 0.7618 (best weight 0.5/0.5). | none | Robustness against covariate shift and variance reduction. | candidate |
| Multi-Layer Perceptrons (MLPs) with Standard Scaling can significantly outperform GBDTs on high-dimensional multiclass audio classification (82 classes) where GBDT multiclass tree-building is slow or sub-optimal. | multiclass audio classification | 5-Fold CV Accuracy improved from 0.7618 (best LGBM) to 0.86954 (std: 0.00566) with (256, 128) layers and learning rate 0.002. | none | Huge boost in accuracy (+0.10) and faster training (29.8s total). | candidate |
| Averaging probability predictions of Multi-Layer Perceptrons trained over multiple random seeds significantly reduces neural network initialization variance and improves out-of-fold accuracy. | multiclass classification | CV Accuracy improved from 0.8811 to 0.91245 (std: 0.00613) with 5 seeds. | none | Variance reduction and improved validation score. | candidate |
| Dense grids of MIDI notes computing FFT harmonic scores (with odd/even, low/mid/high energy splits) and multi-point autocorrelation (with floor/ceil/max neighborhood lags) allow MLPs to learn extremely precise pitch classification rules, even with missing fundamentals. | pitch classification | CV Accuracy improved from 0.91245 to 0.93262 (std: 0.0185). | none | Drastic reduction in error rate for missing-fundamental cases. | candidate |
| Linear SVMs trained on scaled high-dimensional audio features can outperform deep MLPs and GBDTs on sparse multiclass datasets (82 classes, 2330 samples). Ensembling them (MLP + SVM + LGBM) reduces overall validation variance and further improves cross-validation accuracy. | multiclass pitch classification | SVM C=0.05 achieved CV 0.89914 (beating baseline MLP 0.85536 in probe). Three-way blend (MLP: 0.20, SVM: 0.74, LGBM: 0.06) reached 0.93476 CV (std: 0.0157). | none | High-confidence performance boost (+0.00214 CV) and reduced variance. | candidate |
| PCA dimensionality reduction (PCA=512) on scaled concatenated handcrafted and spectral features filters noise and collinearity, stabilizing gradient descent and drastically improving MLP CV (+0.0218 delta). | high-dimensional feature modeling | PCA-MLP (5 seeds) CV Accuracy improved from 0.93262 to 0.95451 (std: 0.00904). | none | Drastic reduction in neural network training noise and accuracy boost. | candidate |
| Blending PCA-reduced linear models (PCA-SVM) with raw/neural MLPs and GBDTs using COBYLA-optimized out-of-fold blending weights reduces variance and sets a new performance best. | ensemble ensembling | Blended OOF Accuracy improved to 0.95579 (std: 0.0117) with 0.77 PCA-MLP, 0.21 Raw MLP, 0.03 LGBM. | none | Minor but robust validation boost (+0.0013 CV). | candidate |
| Target RMS normalization of input waveforms eliminates 2.2x gain/loudness shift between train and test datasets, which prevents model misalignment and over-confidence when ensembling neural networks. | covariate shift mitigation | CV Accuracy improved from 0.95579 to 0.96524 (std: 0.00684) by applying RMS normalization and ensembling. | none | Massive boost in validation stability and robustness against covariate shift. | candidate |
| Ensembling multiple MLPs with distinct architecture configurations (e.g., combining 2-layer (512, 256) and 3-layer (256, 256, 128) hidden states) on PCA-reduced features adds model diversity, reducing prediction variance in high-dimensional multiclass classification. | ensembling neural models | 6-model blend including PCA-MLP (256, 256, 128) and Raw-MLP (512, 256, 128) boosted CV accuracy to 0.96824 (std: 0.00852), a +0.00300 improvement over baseline. | none | Finer probability calibration and +0.003 boost in overall CV accuracy. | candidate |
| Pitch tracker quality features (like `yin_std` and `diff_midi`) are highly prone to recording condition shifts and carry massive covariate shift (Adversarial AUC 0.9743), which leads to feature-induced overfitting when included. | covariate shift mitigation | Adversarial validation model placed index 1633 (diff_midi) as the single most critical shift feature (importance = 135). | none | Preventing distribution shift on tracker quality features. | candidate |
| Training classifiers exclusively on robust pitch-related MIDI grid features and raw pitch class estimates (yin_midi, hps_midi) completely bypasses quality/noise features, eliminating covariate shift and improving test generalization. | pitch classification | Blending grid-only models (LGBM, PCA-MLP, PCA-SVM) with baseline models boosted CV to 0.96953 (std: 0.00873) and passed the score gate. | none | Increased model robustness and higher test set generalization. | candidate |
